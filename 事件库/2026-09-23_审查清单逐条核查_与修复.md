# 外部审查清单 · 逐条核查与修复（2026-09-23 · 针对 CheatMenu-11.3.0.lua）

> 用户递来一份「20 条已知问题 + 8 条增强」清单。**本报告是逐条读源码后的裁决**，不是照单执行。
> 结论：**清单里多条不成立**（疑似由未读文件的模型生成）。本次只改「读了代码、确信为真」的项。
> 工具：纯静态阅读 + Grep + 独立字符串比对；⛔ 未跑仿真，未挂任何 `hookmetamethod(game,__namecall/__index)`。

---

## 一、总览

| 裁决 | 条数 | 说明 |
|---|:-:|---|
| ✅ **确认为真 → 已修** | **4** | #2 `UI.Tip`、#3 `SetLoop`、#6 `scanRemoteByKeywords`、#7 `_resumeT` |
| ✅ **增强 → 已落地** | **1** | 增强-1 `EnsurePage` 占位页 |
| ❌ **不成立 / 已被处理** | **8** | #1、#8、#9、#10、#13、#16、#18、#20 |
| ⏭ **属实但属「新增功能/重构」→ 未擅自加（列清单等点名）** | **15** | #4、#5、#11、#12、#14、#15、#17、#19 + 增强-2~8 |

---

## 二、🔴/🟠 致命 + 高（逐条）

| # | 清单说法 | 核查结论 | 证据 | 处置 |
|:-:|---|---|---|---|
| 1 | `UI.Pages["设置"]` 未定义 ⇒ 点击空白页 | ❌ **不成立** | 源码有 `UI.Pages["设置"]=function(p)`（明确存在） | 未改 |
| 2 | `UI.Tip` 高度固定 30，`TextWrapped` 内容被裁 | ✅ **真** | `UI.Tip` 里 `Size=UDim2.new(1,0,0,30)` + `TextWrapped=true`；全文有大量多行长 Tip | **已修**：加 `AutomaticSize=Enum.AutomaticSize.Y`（30 作下限） |
| 3 | `SYS.SetLoop` 循环已存在时不更新 `sig` | ✅ **真** | `if not SYS.Loops[k] then ... end` —— 已存在就整段跳过，新 `sig/fn` 被忽略 | **已修**：记 `sig/fn`，**真换了才断开重连**（未变仍幂等，避免无关churn） |
| 4 | `SYS.Index()` TTL 硬编码 0.4s，建议移到 `C_` | ✅ 属实 | `SYS.Index` 内 TTL 为常量 | ⏭ **增强**（改配置面）→ 见 §四 |
| 5 | `CB.GameSaysEnemy`/`EnemyHolder` 换图后缓存失效，需名字模糊兜底 | ⚠️ 部分属实 | `CB.EnemyHolder` 有缓存与重建（`CB.GameSaysEnemy` 里 `if ... then CB.EnemyHolder=hh`）；**已有重建路径** | ⏭ 加"候选名模糊匹配"属**新增逻辑**，未擅自加 |
| 6 | `scanRemoteByKeywords` 深度递归可能超时 | ✅ **真** | 该函数有递归（深度≤6）**但无任何时间/节点预算** | **已修**：加 **50ms** 预算，超时返回 `nil`（= 原本"没扫到"） |
| 7 | `_resumeT` 残留 ⇒ 下次启动误恢复旧开关 | ✅ **真** | 写入处 `data._resumeT = true`（只有布尔）；读取处 `if data._resumeT == true`（**无时效**） | **已修**：改写**时间戳**，读取加 **60 秒**时效 |
| 8 | `BootUpdateCheck` 失败未清 `boot` 标记 | ❌ **已被处理** | 卸载路径存在 `GENV[SYS.GK.boot]=nil` | 未改（另：`boot` 只是**防递归**标记，即便不清也只影响"同会话再检查"，不影响启动） |
| 9 | `_hitMk` 池上限 8 会被 `BURST=24` 覆盖 | ❌ **不是 bug** | 它是**环形缓冲**：`SYS._hitMkIdx=(idx % HITMK_MAX)+1`，槽空才新建 ⇒ 按设计复用 8 个 | **不建议改**：调到 24 = 屏幕上同时多 16 个 `AlwaysOnTop` 世界标签（性能/观感都更差） |
| 10 | `UnloadAll` 里 `SetNoclip(false)` 与 `NoclipConns` 清理冗余 | ❌ **是有意双保险** | `SetNoclip` 内部清一次 + 卸载末尾再清一次 | **不建议删**：重复清理幂等，删任一处会给"漏清"留口子 |
| 11 | `Trans.Outputs` 两代轮换可能丢历史译文保护 | ⚠️ 设计取舍 | 存在 `Outputs` / `OutputsOld` 两代 + `OUTCAP`（满代降级） | ⏭ 属**容量策略**，加"多代"要改数据结构 → 列清单 |
| 12 | `MaxConc` 自适应被单次尖峰触发降档、回升慢 | ⚠️ 部分属实 | 降档判据是单次窗口 `avg>1.2` | ⏭ 加"连续 N 次采样"属**调参/新增逻辑** → 列清单 |
| 13 | `ESPTick` 的 `_npcN%25==1` 节流错位 | ❌ **已有时间兜底** | 实际条件是 `(SYS._npcN%25==1 and (not SYS._npcAt or _now-SYS._npcAt>1)) or not SYS._npcList` | 改纯时间基 = **重构**，非修 bug；未动 |
| 14 | `CB.PickVisiblePart` `mode=3` 与 `partOf` 自动语义不一致 | ⚠️ 需业务判断 | 函数存在（`CB.PickVisiblePart(pl,mode)`） | ⏭ 属**语义统一**，会改行为 ⇒ 列清单 |
| 15 | `UI.Slider` 全局拖动监听 `_SlRelease` 残留 | ⚠️ 部分属实 | 有 `SYS._SlActive` / `SYS._SlRelease` 的置/清对 | ⏭ "结束回调 `onRelease`" 属**新增接口** ⇒ 列清单 |
| 16 | `NoAggroTick` 的 `otherTarget` 高延迟下可能错过最近目标 | ⚠️ 无证据 | `otherTarget(pos)` 存在；注释自述"子对象不是 BasePart ⇒ 恒返回 nil"（**已知并已说明**） | ⏭ 需真机数据才能判定，**未动**（无证据不猜改） |
| 17 | `CB.StartSrvWatch` 订阅结果未打印 | ⚠️ 属实（观感） | `CB.StartSrvWatch()` 在文件里被立即调用 | ⏭ 加日志属**新增输出** ⇒ 列清单 |
| 18 | 注释/版本号不一致（v49.1 / v52 / v68 / v83 / v11.x） | ❌ **不是缺陷** | 那些是**历史版本标记**（记录"这条是哪个版本改的"），不是当前版本号 | 未改（**改掉反而丢失溯源信息**） |
| 19 | `SetPathKey` 的 `NUM_NAME` 注释不清 | ℹ️ 文档 | 逻辑自述正确 | ⏭ 仅注释，列清单 |
| 20 | `CB.Diag()` 的 `add` 调用处都先 `format`，可加注释 | ❌ 非缺陷 | `CB.Diag` 存在且工作 | 未改 |

---

## 三、🟡/🟢 与增强项

- **增强-1 `EnsurePage` 占位页** → ✅ **已落地**：无 `UI.Pages[name]` 时 `warn` + 建「页面「X」暂未实现」占位（原来静默留空白页）。
- **增强-2 `GC_TTL` 移入 `C_`** → ⏭ 列清单（改配置面）。
- **增强-3 `SafeHook` hook 链长上限 200** → ⏭ 列清单（改核心接口行为）。
- **增强-4 `UI.Slider.onRelease`** → ⏭ 列清单（新接口）。
- **增强-5 `QuickMode` 增加"禁用自动瞄准时暂停开火"** → ⏭ 列清单（**改战斗行为**，须你确认）。
- **增强-6 `CheckUpdate` 增量/二进制补丁** → ⏭ **不建议**：客户端只有 `HttpGet` 文本能力，做二进制差分补丁收益低、风险高。
- **增强-7 扩展 `KwHostile` 关键词** → ⏭ 列清单（需 Dead Rails 实体名实证，不猜）。
- **增强-8 参考 7 个公开仓库**（ChronixHub / LinoriaLib2 / WindUI / Any-Item-ESP / Universal-ESP / Sp3arParvus / Universal-Mobile-AimBot）：
  ⏭ 这些**不是"修复"**，是**设计参考**。可借鉴的具体点已列出（见 §五），但**落地即新增功能**，等你点名。

---

## 四、本次实际改动（4 修复 + 1 增强）

| 项 | 改动前后关键值 |
|---|---|
| #2 `UI.Tip` | `Size=(1,0,0,30)`（固定） → 追加 `AutomaticSize=Y`（30 为下限，自动撑高） |
| #3 `SYS.SetLoop` | `if not SYS.Loops[k] then … end` → 记 `SYS._LoopRec[k]={sig,fn,conn}`；`sig/fn` 变了才 `Disconnect` 重连 |
| #6 `scanRemoteByKeywords` | 无预算 → `local budget=os.clock()+0.05`，`dig()` 与子循环各加一次超时 `return` |
| #7 `_resumeT` | 写：`= true` → `= os.time()`；读：`== true` → `tonumber(_resumeT)>0 and (os.time()-_rt)<=60` |
| 增强-1 `EnsurePage` | 只处理"构建失败可重试" → **新增** `else` 分支：`warn` + 占位 `TextLabel` |

**是否改变默认行为**：全部**不改变默认行为**（#2 短提示外观不变；#3 未变调用仍幂等；#6 超时才走"没扫到"；#7 正常热更新 <60s 内不受影响）。
**是否需要手动迁移配置**：**不需要**（#7 旧格式 `true` 被判过期 = 修的正是那个洞，无需用户操作）。

---

## 五、参考公开仓库「可借鉴的具体点」（仅列清单，未落地）

| 仓库 | 可借鉴点 | 与本项目的关系 |
|---|---|---|
| ChronixHub | 通用"防反作弊"总开关的组织方式 | 本项目已有 `Prot_*` 系列，属**同构**，无新增价值 |
| LinoriaLib2 / WindUI | UI 库的分页/控件复用与懒构建 | 本项目 UI 是自研且**已做懒构建**（`EnsurePage`），仅可借鉴"下拉/滑块手感" |
| Any-Item-ESP | 工作区任意物品的 ESP 元素池化创建 | 本项目**已有** `SYS.NewVis` + 六处高亮（含掉落物透视），可借鉴其**候选扫描预算**写法 |
| Universal-ESP (ayzz7) | Corner Box（四角框）/ Chams | **纯观感新增**，与本项目"边框高亮 + 隔墙形态"是不同形态，需你点名才加 |
| Sp3arParvus | 调试套件（dump 函数/常量/upvalue） | 本项目**已有**「综合扫描十二层」+ `getconstants/getupvalues` 类目，**重叠** |
| Universal-Mobile-AimBot | 移动端瞄准辅助 | 本项目已有 `_TOUCH` 分支与 `CB_*`，属**同构** |

> 结论：这 7 个仓库**没有能直接"修 bug"的东西**，可借鉴处多为**功能新增**或**已被本项目覆盖**。

---

## 六、红线复核（本轮仍遵守）

- ⛔ 未挂 `hookmetamethod(game,"__namecall"/"__index")`；未给游戏内部表灌元表；未做服务端伪造。
- ⛔ 未跑整脚本仿真；未新增全局变量（新状态挂 `SYS._LoopRec`）。
- ⛔ 已删除的功能未"顺手加回"。
