# ChronixHub 融合清单 v2

> **来源**：`C:\Users\Administrator\Desktop\123123\ChronixHub-main`
> 规模：90 文件 / 2.5 MB · 主脚本 `mainv3.lua`（8658 行）· **79 个功能模块** · UI 库 `ChronixUI Lib.lua`（145 KB）
> 主脚本 UI 规模：**21 页签 / 473 个控件**（含 16 款游戏的专用功能）
> **对照对象**：我们自己的 `CheatMenu-6.5.0.lua`（9 页 / 162 控件）
>
> 提取方式：正则扫 `CreateTab` / `AddToggle` / `AddButton` / `AddSlider`，按页签归组（脚本 `_chronix_scan.py` / `_ours_scan.py`）。

---

## ⭐ 一、新增（它有的、我们没有的）

按「收益 ÷ 风险」排序。**每一项都走我们自己的规矩重写**（`P()`/`T()` 登记连接、节流、可逆、能干净卸载），不照搬它的 `cloneref`/全局变量风格。

| # | 功能 | 来源模块 | 作用 | 为什么值得 | 档位 |
|---|---|---|---|---|---|
| 1 | **路径点系统** | `mainv3` 路径点传送页 | 保存/多路径点/在世界中显示/`Ctrl+数字` 快捷传送/删除 | 我们只有"保存当前坐标+自动回点"单点；多点是刚需（换点刷怪/跳图） | ⭐ 强推 |
| 2 | **服务器查询器** | `ServerFinderModule` | 查当前游戏的**公共服务器列表**（人数/延迟）并一键加入 | 纯只读（Roblox 官方 API `HttpGet` + `TeleportService`），我们完全没有 | ⭐ 强推 |
| 3 | **滤镜控制器** | `mainv3` 滤镜页 | 饱和度/亮度/对比度 + **色盲模拟** + 后处理特效开关 | **纯客户端视觉**（改 `Lighting` 后处理），零风险，观感提升明显 | ⭐ 强推 |
| 4 | **聊天接收器** | `mainv3` 聊天页 | 实时列全部聊天消息 + 一键复制 + 清空 | 我们有聊天**翻译**但没有"消息列表"；配合翻译是天然一对 | ⭐ 强推 |
| 5 | **音频控制器** | `mainv3` 音频页 | 本地调游戏内总音量 / 逐条静音 | **只改自己听到的** `Sound.Volume`，不影响别人 | ✅ 建议 |
| 6 | **光照档位包** | `mainv3` 工具页 | 夜视 / 超级夜视 / 随身灯笼 / 超级光明 / 禁用雾效 / 禁用全局阴影 | 我们有"全亮"，但缺**分档**；夜间图/雾图差别很大 | ✅ 建议 |
| 7 | **只读可视化诊断** | `mainv3` 工具页 | 显示碰撞箱 / 显示隐藏部件 / 打印当前坐标 / 模型信息查询 | 只读不改，排障用（我们已有 Ctrl+点击看部件，可并到一组） | ✅ 建议 |
| 8 | **灵魂出窍 / 旁观视角** | `mainv3` 工具页 | 相机脱离角色自由飞（角色留在原地） | 纯本地相机，配合已有的自由视角/透视，侦察价值高 | ✅ 建议 |
| 9 | **自动跳跃 / 边缘跳跃** | `MovementModule` | 遇障碍自动跳 / 走到边缘自动跳 | 纯本地移动辅助，挂机与赶路都用得上 | ✅ 建议 |
| 10 | **锚定到世界** | `mainv3` 工具页 | 把角色钉在原地（不随物体移动） | 本地 CFrame 锚定，做精细操作时有用 | ✅ 建议 |
| 11 | **Roblox 版本 + 线程身份查询** | 执行器查询页 | 打印 Roblox 客户端版本、执行器状态、当前线程身份 | 补在我们已有的「执行器能力自检」旁边，一次看全 | ✅ 建议 |
| 12 | **本地音乐播放器** | `mainv3` 音乐页 | 用 `rbxassetid` / 直链放歌，带收藏夹、可视化 | 纯本地 `Sound`。**只做"自己听"，不外放给别人** | ⚠️ 可选 |
| 13 | **本地时间切换** | `mainv3` 工具页 | 白天/黑夜 | 改本地 `Lighting`（若游戏同步则无效，属于"试了才知道"） | ⚠️ 可选 |
| 14 | **望远镜** | `mainv3` 工具页 | 一键放大 FOV（按住看远处） | 我们有 FOV 滑块，这个只是"快捷键 + 复位" | ⚠️ 可选 |
| 15 | **UI 显示控制** | `mainv3` 工具页 | 隐藏所有 UI / 显示不可见 UI / 禁用购买提示 / 移除广告牌 | 部分与我们"删广告 GUI"重合，可合并成一个可逆开关 | ⚠️ 可选 |
| 16 | **自定义称号** | `ChatTagModule` | 聊天栏前缀/字号/加粗/斜体 | ⚠️ **必须先确认是否只本地可见** | ⚠️ 待确认 |

---

## 🔧 二、补强（我们做了，但它那版有更细的做法）

| # | 我们的功能 | 它的做法 | 具体补强点 |
|---|---|---|---|
| 1 | 加速 / 超级跳跃（滑块） | 基础设置页每项都是 **`滑块` + `锁定开关`** 成对出现（锁移速/锁跳跃/锁血量/锁重力） | ★ **给我们的数值滑块加"锁定"** —— 现在游戏脚本一改移速就回去了，没有回写保护。**这是最值的一条** |
| 2 | 玩家名字标签 | 基础设置页有 **`控制玩家名称显示距离` / `控制生命值显示距离` / `始终显示玩家生命值`** 三个独立控制 | 我们 6.5.0 刚给标签加了"距离"，但还缺**距离上限**和**血量始终显示**两个开关 |
| 3 | 视野 FOV | `ZoomModule` 是**平滑缩放**（带过渡） | 我们的 FOV 滑块是突变；加缓动即可，纯客户端 |
| 4 | 飞行（单一实现 + 模式 Cycle） | 5 种独立实现：`FlyModule` / `CframeFly` / **`VehicleFly`(载具飞)** / **`MinecraftCreativeFly`(创造飞)** / `AirWalk` | 补**载具飞**档（对坐着的载具施力）与**创造飞**档（视角方向即移动方向） |
| 5 | 透视（玩家/NPC/掉落物/可交互/门陷阱） | 它还有 **`TCPHighLight`（透视触点实例）** 与 `MovableHighlighter-NM`（高亮**会动的部件**） | 我们已有"可交互道具透视"；可再补**"动态部件高亮"**这一类 |
| 6 | 执行器能力自检 | `UNCAndWUNCGet`（65 KB，完整 UNC/WUNC 检测）+ 执行器查询页 | 我们已有自检；补 **Roblox 版本 + 线程身份**两项输出即可 |
| 7 | 帧率优化（一键 + 剔除距离） | `GraphicsEnhancer` / `LowQualityMode` / 禁用3D渲染 / 性能模式 | 把我们的一键拆成**3 档**（轻/中/狠），并补"禁用全局阴影/雾效" |
| 8 | 客户端清理（垃圾部件 + 广告 GUI） | `MovingPartCleaner` / `GuiDeleter` / `Deletermodule` / `DeleteTool` | 补 **Ctrl+点击 直接删**（我们已有 Ctrl+点击**看**，删是自然延伸 —— 但要加二次确认） |
| 9 | 快捷键 / 热键设置 | 路径点页支持 **`Ctrl+数字` 记忆位** | 补"数字键位直达"的通用机制 |
| 10 | 传送（模式 Cycle） | 玩家控制页有 **`传送` / `缓动` / `步行` / `寻路` 四个独立按钮** + 缓动速度滑块 | 我们已有传送方式 Cycle；补**缓动速度**滑块与**寻路**档 |
| 11 | 界面翻译 | `TranslationModule`（12 KB） | 只对比它的**缓存命中/并发**写法；**不换我们的实现**（我们的 v70 版更强） |
| 12 | 菜单/UI | `ChronixUI Lib`（145 KB，21 页全靠它） | ⚠️ **不换库**。仅可借鉴它的**通知系统**（`SystemNotification`）与页面动效 |

---

## ✅ 三、上一轮已融合（对照，别重复做）

- `6.4.0`：**假人高亮** · **防掉虚空** · **空中走** · **执行器能力自检** · **F3 调试屏** · **客户端清理组**（清垃圾部件 / 删广告 GUI）
- `6.5.0`：**名字标签补距离** · **卸载对称补齐** · **配置导入/导出**
- 更早：**落脚点指示** · **Ctrl+点击看部件信息** · **瞬间交互**（ProximityPrompt 按住时长→0）

---

## 📋 四、执行批次

| 批次 | 内容 |
|---|---|
| **第 1 批** | ① 数值**锁定**（移速/跳跃/重力）② 路径点系统 ③ 服务器查询器 |
| **第 2 批** | ④ 滤镜控制器 ⑤ 聊天接收器 ⑥ 光照档位包 |
| **第 3 批** | 音频控制器 · 灵魂出窍 · 自动跳跃 · 只读可视化诊断 |

> 每项：读它的实现 → 按我们的风格重写（`P()`/`T()`/节流/可逆）→ 混进现有页面 → 只跑 `push_now.py --fast`（**不跑仿真**）→ 动了 README 再 `push_docs.py`。
