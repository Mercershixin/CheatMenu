# CheatMenu 更新记录 · CHANGELOG

> **换账号 / 新环境时看这份就够了** —— 当前该同步到什么状态、这版改了什么、怎么让它生效。
> `version.txt` 里的数字 = 当前版本号；每次发行都会更新本文件。

---

## 4.10.0 · 2026-09-18

### 📐 菜单「必须正中间」+「PC/手机/平板都能拖」—— 这次挖到根因了
上一版没修到根上。这次两个根因都定位了，并加了**运行时自动兜底**。

**① 居中**（截图里菜单偏左偏上、还整体缩小）
- **根因**：执行器的 **`gethui()` 返回的容器不保证是全屏**。菜单的位置与缩放都是按「父容器」算的，
  父容器一小 → 菜单被挤到**左上角**、还被**整体压小**。**跟手机/电脑无关，PC 一样中招。**
- **修法（三条一起上）**：
  1. **不再用 `gethui()` / `get_hui()`** → 固定放 **CoreGui**（全屏；写不进去就退 PlayerGui）；
  2. **锚点居中**：`AnchorPoint(0.5,0.5)` + `Position` 全用 Scale —— 缩放不会把中心带偏；
  3. **运行时自检**：算一下菜单本该落在哪（`(视口 − 宽×缩放) / 2`），实际差 >8px 就说明父容器不是全屏
     → **自动退回 PlayerGui** 并打印日志，保证一定居中。
- 新增 **`⊙` 一键居中按钮**：拖歪了点一下回正中。

**② 拖动**（拖不动）
- **根因**：拖动把手原来是 **Frame**。Frame 要收输入得靠 `Active=true`，而这个属性在不同执行器 /
  叠放层级下并不一致 —— 实测"给了 Active 还是拖不动"。
- **修法**：把手改成 **TextButton**（**按钮天生接收输入**，不依赖任何属性）；PC 鼠标 + 手机手指都支持。
- 顺带修掉「拖不满 / 不跟手」：**UIScale 会把 Position 的 Offset 一起缩放**（鼠标走 100px 菜单只走 60px）
  → 现在除以当前缩放，做到 **1:1 跟手**。

### 📡 延迟：测量 / 低延迟模式 / 自动跳到低延迟服务器（新增）
> 看过公开实现（FlamesHub 的 SAB Auto Joiner、BloxFruit Hop）后落地。
- **实时延迟**：`GetNetworkPing()`（取不到退 `Stats.Network.ServerStatsItem["Data Ping"]`）。
- **低延迟模式**：关阴影 / 环境反射 + 开帧率优化 —— **完全可逆**，关掉原样还原。
- **自动跳低延迟服务器**：拉 `games.roblox.com/.../servers/Public` 列表 → 挑未满且没试过的 →
  `TeleportToPlaceInstance` 跳过去 → **到了再测 ping**，仍超阈值就继续跳，**最多 `HopMaxTry` 次（默认 4）自动停**。
- **防死循环**：已跳次数用 `queue_on_teleport` 带到新服（`_G.CM_HOPN`），到上限就不再跳。
- ⚠️ **诚实边界（重要）**：
  - **ping 由你到 Roblox 数据中心的物理距离决定，客户端改不了**。能做的只有两件：
    **换到更低的服务器**、**压低帧时间**（帧时间不是 ping，但操作更跟手）。
  - 服务器列表 API **没有可靠的 ping 字段**（公开实现也不用它）→ 只能"跳过去再测"。
  - **本地版没有网络地址**：跳服后脚本**不会自动回来**（需重新执行一次加载器）；
    网络版 / 加载器版会用 `queue_on_teleport` **自动重注入**。

---

## 4.9.0 · 2026-09-18

> 📌 **版本号更正说明**：本节内容当时按"补丁版"写成了 `4.8.1`，但实际发行时脚本按**默认（功能版）**走了
> → 用户拿到的是 **4.9.0**。这里按**实际发行号**改回，避免 `version.txt` 与本文顶部对不上。
> （教训已写进 README 第 1 条：修 bug 必须显式加 `--patch`。）

### 🖱 修「菜单拖不动」—— 真根因：拖动把手根本收不到输入
- 拖动把手 `top` 是个 **Frame**。Roblox 里 **Frame 不是按钮，默认不接收输入事件** ——
  没给它 `Active=true`，那段 `top.InputBegan` **永远不会触发**。
  → 所以菜单**从头就没法拖**，跟鼠标/触屏无关（**PC 也拖不动**就是这个原因，你说对了）。
- 现在 `top.Active=true` → **PC 鼠标 + 手机手指 都能拖**。

### 🧹 删除「🫥 深度隐藏菜单」（你说没用）
- 它把菜单挂到执行器的 `gethui()` 容器，而**那个容器不保证是全屏**；菜单位置是按父容器尺寸算的
  → 一挂上去菜单就跑到**左上角**、还被压小。**PC 上同样中招** ——
  你说的"电脑也跟着左上角去了"就是这个开关造成的。
- 现在**固定挂 CoreGui**（全屏 → 位置永远正常，且比 PlayerGui 隐蔽）：
  界面上不再有这个开关，配置项 `SafeGui` 也一并删掉。

### 🐛 修「点了卸载，右边 ☰ 浮动按钮还在」
- 浮动按钮是**独立的 ScreenGui**，不挂在菜单 ScreenGui 下面 → 销毁菜单**不会**连带销毁它。
- 卸载流程现在**显式销毁**它 → 卸载干净。

**没有改动、保持原样的部分**：菜单仍是**锚点居中**（`AnchorPoint(0.5,0.5)` + 全屏 CoreGui，
缩放不会把它带偏）；缩放仍是**只缩不放、保证整块塞进屏幕**（不会出现按钮跑到屏幕外点不到）。

---

## 4.8.0 · 2026-09-18

### 翻译提速：本地短语表从 9 条扩到 58 条（短句 0 毫秒直译，不走模型）
- **实测结论**：「多条合批翻译」反而更慢（生成是串行的，总字数不变）—— 真正能让大片英文瞬间变中文的
  只有【本地直译】：命中就直接返回，**0 毫秒、不占 GPU、不请求模型**。
- 本地词表补了 **178 条**（单词 129 + 短语 49）：
  高频按钮（开始/继续/返回/关闭/确认/取消/保存/设置）、货币（金币/宝石/经验/生命）、
  战斗（爆头/击杀/连杀/复活/胜利/失败/换弹/弹药）、提示（确定吗？/请稍候/加载中/余额不足/已售罄）、
  社交（加好友/组队/交易）、任务（每日/每周/完成/解锁/升级）……
- 之前的数值+货币识别（如 ）继续生效。

---

## 4.7.0 · 2026-09-18

### 修手机上「菜单 UI 跑左上角/偏位」的真根因
- 之前居中靠的是  —— 用【偏移量】做的居中。
  而菜单上有个自动缩放（小屏会缩），**缩放会把位置偏移一起缩掉** → 偏移变小 → 菜单越缩越偏、跑到左上角。
- 现在改成【锚点居中】： + （全用比例，没有偏移量）。
  **缩放只影响大小、不影响锚点居中** → 任何屏幕、任何缩放倍数下都稳稳居中。
- 打开动画那处同样的偏移写法也一并改了。

---

## 4.6.0 · 2026-09-18

### 修手机版两个实机 bug（用户手机实测截图）
**现象**：手机注入后菜单**挤在左上角、还被压得很小**，而且**拖不动**。

**根因（两个完全独立的原因）**：
1. **菜单跑角落** —— 菜单被优先挂到了执行器的隐藏容器（`gethui()`）。
   **有些执行器那个容器不是全屏**，而菜单的居中算法是`父容器宽的一半 - 菜单宽的一半`，
   父容器一错 → **位置全错、还跟着被压小**。
2. **拖不动** —— 拖动判定只认**鼠标事件**（左键按下 / 鼠标移动），而**手机上根本没有鼠标事件**，
   触摸屏发的是**触摸事件**，所以一个都匹配不上，自然拖不动（☰ 悬浮按钮同理）。

**修法**：
1. **默认改挂 CoreGui**（全屏、位置正常，也比挂 PlayerGui 隐蔽）；想要更隐蔽可在设置页**手动开**「深度隐藏菜单」，
   那时才挂隐藏容器，并明确提示"位置可能偏"。设置页还带一个**实时状态标签**，一眼看到当前是哪种。
2. **拖动判定补上触摸事件**（菜单本体 + ☰ 悬浮按钮**两处都改了**）→ 手机上现在**能拖能点**。

**顺带修掉自己引入的一个错**：写「深度隐藏菜单」时把开关组件当参数组件用了（开关的第 3 个参数必须是**开关名**，
我传了个函数），导致仿真遍历开关表时报错。已改成按钮切换（这个设置本来就是参数不是开关）。

---

## 4.5.0 · 2026-09-18

### 设备适配补强（电脑 / 平板 / 手机）
**先说结论**：之前已经有不错的底子（v108/v120 做的）—— 自动识别触屏、留出刘海/顶部栏边距、
按屏幕自动缩放、屏幕尺寸变化实时跟；手机上早就有个 ☰ 悬浮按钮（能拖走、点一下开菜单），**手机上能正常打开菜单**。
这次补的是剩下的缺口：

| 补了什么 | 为什么 |
|---|---|
| **触摸设备按钮加高**：普通按钮 40→**46**，开关行 44→**50** | 手指比鼠标粗；小屏还要再缩放，40 缩到 0.7 就只剩 28 像素，很容易点不准 |
| **触摸设备默认开「帧率优化」** | 手机/平板性能弱，默认不开容易掉帧（你自己存过的配置仍然优先）|
| **悬浮按钮尺寸自适应**：小屏 52 / 大屏 58，位置避开右上角 | 有的游戏右上角本来就有按钮，会挡住 |
| **综合扫描新增设备信息**（触屏/键盘/鼠标/视口档）| 以后排查"手机上按钮点不到"这类问题，一眼就能看到设备状态 |

**关于"电脑"**：电脑就是"有键盘有鼠标"的路径 —— 不受上面改动影响（按钮还是 40，走 G 键开菜单）。
笔记本小屏也吃"小屏适配"那一档缩放。**两种设备用的是同一套代码，靠自动探测分流，不需要你手动切换。**

---

## 4.4.0 · 2026-09-18

### 用三种真实战斗场景压测，抓出并修掉一个真 bug
新增仿真场景：**多人乱战（6 人无队伍）/ 团队战（3v3）/ 个人战（1v1）**，共 14 项检查，全部通过。

**抓到的真 bug：团队战的队伍过滤其实一直没生效**
- 现象：队伍判据每次都得出"这个游戏没有队伍"的结论 → **永远不排除队友**（团队战里会打自己人）。
- 根因：我写「判据有效性检测」时取错了返回值 —— 工具函数返回的是`(成功与否, 结果)`两个值，
  我写成了取第二个（结果是 nil），于是 `(结果 and 有两支队)` 永远是假。
- 修法：只取第一个值（成功与否）。修完团队战判据变成 `true`，3 项检查全过。
- **对你的实际影响**：你玩自建房（没有队友），所以**没感觉**；但**万一玩团队战就会打自己人**。

**顺带把仿真环境修扎实了**（这几处是测试写法问题，不是脚本缺陷，记下来免得以后再踩）
- 造的假玩家缺 `Humanoid:GetState()` → 生死判定报错 → 全部被判成死人（所以一开始一个都选不中）
- 战斗循环有「总闸」：**所有战斗开关都关时会直接返回、连选人都不跑** → 测试必须先开一个开关
- 战斗循环用的绑定要在**启动之后**重新取（启动会解绑重绑，旧引用静默失效、扫描次数一直是 0）

---

## 4.3.0 · 2026-09-18

### 查漏补缺：清理"透明隐身"的残留代码
- 「透明隐身」在 v82 就按你的要求删掉了（只能本地生效、别人仍然看得到你），但**代码没删干净**：
  开关声明、`SYS.SetInvisible` 整个函数（30 行）、**卸载时还在调用它**、以及界面提示里还写着"透明隐身=本地"（误导）。
- **现在全部清掉**：开关声明 / 函数 / `InvisSaved`、`InvisConn` / 卸载调用 / 提示文字。
- **其他"已删功能"逐个核对**（都确认过，无需清理）：

| 项 | 结论 |
|---|---|
| 方框透视 / 视场圈开关 / 距离标签 / 目标高亮框 / 锁定保持 / 锁人速度 | **已经清干净**（v3.7.2 那轮清的）|
| 静默瞄准 / 快照瞄准 | **有意保留**（脚手架 + 老配置强制关掉的逻辑，注释已说明）|
| 快照瞄准的 3 个参数 / 视场圈的 2 个参数 | **有意保留**：老配置迁移（把旧值改成新默认）+ 战斗诊断里要打印它们 |

### 顺手：给两个"预留接口"补上说明，免得以后被当成死代码删掉
- `SYS.Invoke`（调用需要回包的远端函数）与 `SYS.REventU`（高频信号用的不可靠远端事件）——
  目前**没有任何功能在用**，是做"自动领奖/自动购买"这类需要回包的功能时的接口。已在注释里写明"预留，别删"。

---

## 4.2.0 · 2026-09-18

### 扫描大改：合并成一个按钮，八层一次扫完（用户要求）
- **删掉单独的「🔬 实验室」页**（用户：不用单独排布，也不好找），扫描功能**全部搬进「功能」页**。
- **`🔍 综合扫描` 一个按钮**，输出**按层分行**打到控制台（F9），并带 `📋 复制扫描摘要到剪贴板`：

| 层 | 扫什么 | 你能得到什么 |
|---|---|---|
| **A 通信层** | 全部 Remote（RemoteEvent/Function、Bindable、ProximityPrompt、ClickDetector）| **能触发哪些接口** —— 并按 `⚔战斗 / 💰经济 / 📦物品 / 👥社交 / 🏃角色` 分组挑出**能做功能的关键词**（原来的「扫描游戏 Remote」按钮已并进来）|
| **B 代码层** | `getgc(true)` 的函数/表 + 按**归属脚本**分组 | 游戏有哪些函数；**名字可疑的**（damage/fire/aim/kill…）列出来可直接拿去观察 |
| **C 脚本层** | `getscripts` / `getloadedmodules` / **`getrunningscripts`** | 跑了哪些脚本/模块，**正在运行**的是哪些 |
| **D 实例层** | **`getnilinstances`**(父级为 nil 的隐藏对象) + Workspace 规模 + 角色/Highlight/Prompt 统计 | **游戏藏起来的东西**往往在这里 |
| **E 数据层** | 自己/他人的 **Attribute 全字段** + 角色 Attribute + **leaderstats** | `@Health/@Team/@State` 这些就是从这里来的；能发现还没用上的新字段 |
| **F 连接层** | `getconnections` | 游戏自己挂了哪些事件监听（可查看/挂起）|
| **G 环境层** | `getgenv`/`getrenv`/`getreg` + **线程身份** | 判断脚本跑在什么权限上下文下 |
| **H 反查层** | `getcallingscript` + 函数 **upvalue 概览** | 这段代码是被谁调起来的、闭包里有什么 |

- **进阶区保留**（同一个页面下方）：`观察某个函数`（hook 一层**只记录参数、原样转发、绝不改行为**）+ 调用记录 + 调用链探测 + 一键还原。
- 新增执行器全局（getrunningscripts/getgenv/getrenv/getreg/getthreadidentity/getcallingscript/getnilinstances/getconnections/setclipboard…）
  已进白名单（53 个名字）；仿真桩补了 `GetAttributes`（真实 Roblox 有、桩缺了会让数据层报 nil 方法）。

---

## 4.1.0 · 2026-09-18

### 修：锁不上敌人（队友误判 + 隔墙射线太严）
- **「不打队友」把敌人也判成队友** → 谁都锁不上。
  根因：队伍判据改成了 `@Team` Attribute，但**自愈逻辑还在看 `Player.Team`**（本游戏恒 nil）→ 自愈永不触发；
  而 `@Team` 在所有人身上取值相同（自建房/混战类游戏本来就没队友）→ 全服被排除。
  改动：① 统一判据 `CB.TeamKey()` ② 新增**判据有效性检测**（全服同队 = 该游戏不用队伍分敌我 → 不排除）
  ③ 自愈改用同一判据 ④ **「不打队友」默认改为关闭**（你的场景没有队友，开着纯负作用）。
- **敌人离开一定距离就不锁** → `clearShot` 原来**只看中心一条射线**，远处敌人只露半身/头皮时中心被掩体挡住
  → 直接判"看不见"。改成**子弹缝多点采样**（中心+头皮+左右上下+四个斜上角共 10 点），
  只要有一点通就算"有缝可打"，于是**隔着建筑缝隙也能锁上、也能开枪**。

### 新：近距离补刀（贴脸自动按 F）
- 和敌人贴脸时枪常打不中（准星/弹道问题）→ 新增「🔪 近距离补刀」：距离 < 设定值（默认 9 格）时
  自动按 **F** 用近战收掉，节流 0.35 秒。默认关闭，战斗页可开 + 可调距离。

### 新：🔬 实验室页（执行器高级 API 实验区）
- 研究 `getgc(true)` / `hookfunction` / `pcall` + immediate caller，全部**只读 / 可撤销**：
  ① 执行器能力探测 ② GC 扫描（函数/表数量 + 按归属脚本分组）③ 可疑函数清单（名字含 damage/fire/aim/kill…）
  ④ 已加载脚本/模块清单 ⑤ **包一层观察**某函数（只记录参数与返回值，原样转发**不改游戏行为**）⑥ 调用链探测
- 边界：**只做观察，不改游戏逻辑，不碰反检测**，全部只影响本客户端。

### 工程
- **Remote 多路径解析**：原来 `REvent/RFunction` 只认 `Shared.Packages.Network.rev_/ref_<名字>`，
  而本游戏 Remote 挂在 `ReplicatedStorage.Remote.<服务>.<名字>`（抓包实锤）→ `Fire/OnRemote` **一直是死的**。
  现在四级兜底（实例/完整路径/递归找/老式网络库）+ 结果缓存；并新增 `Invoke`（RemoteFunction）与 `UnreliableRemoteEvent` 支持。
- `CB_MeleeDist/CB_MeleeGap` 归位到参数表（原来错放进开关表）；执行器全局白名单更新到 46 个名字。
- 门禁 **11/11 全过**；代码逻辑检查（风险审查 269 条）逐条核实**均为误报**（表构造字段 / 单行 while 的 wait）。

---

## 4.0.0 · 2026-09-18

### 翻译调教（准确率 18/20 -> 19~20/20）
- **SYS_PROMPT 加游戏术语表**（29 条固定映射）：XP→经验、Level→等级、Respawn→**复活**（原来译"重生"）、
  Rank→**段位**（原来把 Gold III 译成"金币III级"）、Inventory→**背包**、Streak→**连杀**、Reload→**换弹** 等。
- **货币符号规则强化**：`$` / `€` / `¥` 与 `Robux` 一律**原样保留**（不再把 `+500 $` 译成"+500 美元"）。
- 术语固定映射 = 同一个英文词每次都译成同一个中文词，界面读起来一致。

### 性能（实测调优）
- **服务端并发 24 → 32 槽**（`-c 12288` → `-c 16384`）：48 条并发实测 **2.50s → 2.38s**（105 → 110.6 tok/s），
  显存 5517 → 5804 MiB（仍留 2.4GB 给游戏，边玩边翻不 OOM）。
- 保留 `-b 4096 -ub 1024 --cache-reuse 256 -sps 0.2`（大 batch + prompt 前缀缓存复用）。

### 工程
- **源码文件名跟版本号走**：`CheatMenu_v68.lua` → **`CheatMenu-<版本>.lua`**（本版 = `CheatMenu-4.7.0.lua`）。
  新增 `rename_version.py`：升版时一键改文件名并更新所有脚本引用。
- **README 补「本地测试 / 模拟方法」章节**（给智能体接手用）：`verify_all.py` 一条命令 7 步门禁、
  仿真台原理、桩的保真度坑（JSONDecode 假解析 / os.clock 真时钟 / 必须走真实触发链路）都写清了。
- 删除仿真台里**已失效的「配置管理」用例**（该功能已确认不要），门禁恢复干净。
- **5 轮「全面模拟 + 循环修复」全部通过**（每轮 11/11 步）。

---

## v3.10.3 · 2026-09-18

### ✈️ 修「飞行 CFrame 模式只有加速、飞不起来」

CFrame 模式原来逐帧写 `root.CFrame`（"逐帧瞬移"），但被服务端位置校验拉回——尤其垂直方向，每帧那点向上的位移被重力 + 服务端校正完全抵消，于是只剩水平"加速"、垂直"飞不起来"。现在 CFrame 模式也改用 **LinearVelocity 驱动**（和 Align 同一条路），用 `MaxForce=∞` 的 LinearVelocity 真正抵消重力 + 提供三轴速度，三种模式都能稳定悬浮/升降。

### 🔫 武器透视 → 头顶武器标记

原「手持武器透视」（显示手上武器名 + 槽位字符串）改为「**头顶武器标记**」：检查**背包（Backpack）或手上**有没有武器（Tool），有就在头顶挂一个醒目的红色武器名标记（如 `🔫 武器名`），没武器不留标签。

### 🎨 玩家透视改色（队友绿 / 敌人红）

- **队友 = 绿色，敌人 = 红色**（高饱和、够醒目），替换原来的"队友青绿 / 敌人金色"。
- **队伍判断改用 `@Team` Attribute**（这个游戏 `Player.Team` 是 nil），与战斗模块的队友判断统一。
- **隔墙区分但不偏移颜色**：隔墙的人填充更透明（能看穿墙，一眼知道"在墙后"），可见的人填充更实；**颜色 RGB 值始终不变**，不淡化、不变色相。

---

## v3.10.2 · 2026-09-18

### 🩸 再修「活人被误判死亡」（第二处根因）

2.txt 诊断实锤：`wswyx12345` 满血站着（`@Health=100 @State=Stand`、`Humanoid.Health=100`）却被判死。根因是**死亡事件（`GameService.Killed` 等）在交易大厅/回合切换时误发或参数误标**，把活人名字写进 `DeadAt`，而他的角色实例没换 → 8 秒 TTL 内被无条件判死。

修复：**Attribute 给出明确的「活着」证据（`@State` 是活跃状态且 `@Health>0`）时，直接清掉 `DeadAt` 误标**——实时权威数据高于历史死亡信号。

同时把诊断输出精确到判据（到底是 `@State==Dead` / `@Health<=0` / `DeadAt` / `Humanoid` 哪条判死的），下次再有问题一眼定位。

### 🐛 修「传送页构建失败」

日志报错 `Unable to assign property BackgroundColor3. Color3 expected, got nil`——根因是观战按钮用了 `CY.orange`，但颜色表里根本没定义这个字段。已补上 `orange` 颜色。

### 📦 事件库补充

归档了完整 Remote 清单（287 RemoteEvent + 146 RemoteFunction，比上次 119 个更全），含 `CombatService.Action`（战斗动作权威入口）、`BackpackService.TryEquip/TryUnequip`（装备切换）等战斗相关信号。

---

## v3.10.1 · 2026-09-17

### 🩸 战斗判定改用「权威 Attribute 数据」（修活人被误判死亡的真根因）

用户贴的「物品栏来源诊断」实锤：这个游戏的**血量 / 状态 / 队伍 / 护盾 / 换弹信号全在 Player 的 Attribute 里，不在 Humanoid / Team 上**。之前一直用 `Humanoid.Health`、`Humanoid:GetState()`、`Player.Team` 判定，导致：

- **满血活人被误判成已死亡**（`Humanoid.Health` 不可信，游戏把真血量放在 `@Health`），索敌全空。
- **队伍判断失效**（`Player.Team` 是 nil，队伍其实在 `@Team`，如 `Team3`），队友过滤失效。

本版全部改读 Attribute 权威值（Humanoid/Team 只作兼容别的游戏的回退）：

| 判据 | 旧（不可信） | 新（权威） |
|---|---|---|
| 生死 | `Humanoid.Health` / `GetState()` | `@State=="Dead"` + `@Health<=0` |
| 护盾 | `ForceField` | `@Shield` / `@TempShield > 0` |
| 队伍 | `Player.Team` | `@Team`（`Team1`/`Team3`） |
| 换弹降权 | 手上有没有 `Tool` | `@combatPaused`（换弹/受控/无法攻击）+ 手上 Tool 双信号 |

### 🎯 索敌威胁度升级（换弹降权用权威信号）

"正在瞄我的人"里，**能立刻开枪的（`@combatPaused≠true` 且手持武器）最优先**；正在换弹/受控/收枪的降权。优先级链：正在瞄我且能开枪 → 正在瞄我但换弹/收枪 → 我指定的目标 → 最近的 → 屏幕中心。

---

## v3.10.0 · 2026-09-17

### 🎯 战斗模块大改（索敌更聪明 + 修"活人被误判死亡"）

**修：活人被误判成已死亡 → 索敌全空**（用户报"选人结果=无，所有人都被挡：已死亡/无角色，明明 Health=100"）。根因：死亡事件的参数解析把武器名/击杀者名等无关字符串也当成了"死亡玩家名"写进 `DeadAt`。现在只认**当前服务器里真实存在的玩家名**，且一次事件若带多个玩家名（广播型事件）直接忽略——宁可漏判死人，也不误杀活人。

**自动开火适配自动瞄准**：跟随速度拉满（`CB_Smooth≥1`，即"一键开战"的默认）时相机已瞬时对准目标，直接开火、不再走"屏幕投影容差"判定（那一步会因"瞄准点≠部位中心/远距离小目标"误判没压中，表现就是"瞄准对上了却不开火"）。

**索敌优先级升级（威胁度排序）**：正在瞄我的人里，**手上拿着武器（能立刻开枪）的最优先**；正在换弹/收起武器（手上没 Tool）的自动降权。优先级链现在是：正在瞄我且有枪 → 正在瞄我但没枪 → 我指定的目标 → 最近的 → 屏幕中心。

### 🕳 藏身升级：支持「藏天上」+ 左右/前后偏移

藏地下隐身新增三档设置：
- **藏身方向**：地下 / 天上（藏天上=把你抬到头顶高处，相机仍留地面）。
- **左右偏移**（正=右 负=左）、**前后偏移**（正=前 负=后）：开启时往旁边挪一点。

### 👁 观战入口更直观

观战从"右键玩家列表"（太隐蔽，用户没发现）改成**每个玩家一行：名字 + 「传送」 + 「观战」两个显式按钮**。

### 🔫 子弹射线方向修正

子弹射线（Tracer）原来从**第三人称相机**位置沿相机朝向画，方向是错的。现在改成从**角色眼睛/头部**沿**角色朝向**（枪口指向）射出，画的是人物正面打出去的弹道线。

### 🎥 新增「强制第一/第三人称」

强制把相机锁成第一人称（`LockFirstPerson`）或第三人称（经典可拉远），每 0.5s 兜底抢回、防游戏脚本改回去；关闭/卸载时还原原始视角。

### 🖱 鼠标彻底修复（关 UI 后不再锁死/隐形）

关菜单 / 关自由视角 / 卸载后，鼠标**统一恢复成"可见 + 可移动"（Default）**，不再去追"打开前那一刻"的旧值（那些旧值常是 `LockCenter`，就是锁死的根因）。

### ✈️ 飞行 CFrame 模式修复

CFrame 飞行模式原来没接管 Humanoid 状态机，角色控制器每帧把角色拽回地面 → 没效果。现在和 Align/BodyVelocity 一样接管状态机，位移真正生效。

### 📍 传送 / 自动回点优化

- 鼠标传送距离太短：分步插值改成**大步跳（最多 4 步）+ 每步落地补刀**，单帧瞬移覆盖更广（`TPMaxStep` 60→300）。
- 自动循环 TP 与自动回点不同步：去掉 0.45s 节流 + 冷却从 2s 降到 0.5s，离开保存点后更快被拉回。

### 🧹 清理（按用户要求删除）

- 删掉「配置管理 / 多套存档」（Save/Load/Delete/ListProfile）。
- 删掉「RemoteSpy 抓包」功能（保留静态扫描）。
- 删掉「物品栏来源诊断」按钮。

---

## v3.9.1 · 2026-09-17

### 🎯 战斗模块补强（基于游戏 Remote 清单）

补订了上一版漏掉的**客户端专属击杀信号** `GameService.GameClient.Killed`——有的游戏只有这条击杀事件、没有外层 `GameService.Killed`，补上后这类游戏的"锁死人"判定更准。

> 说明：`EntityService.BeDamaged/Heal` 是"受击/回血"信号、**不等于死亡**，若硬塞进死亡判定会把"刚被打但没死的人"误排出去，所以**故意不接**。

---

## v3.9.0 · 2026-09-17

### 🔄 恢复老执行器兼容 + 反陷阱/鼠标修复

这一版主要是**把之前"安全化"那批改动里对老执行器不友好的地方调回来**，并修了几个 bug。

**恢复 / 兼容**
- ✈️ 飞行默认执行器改回 `BodyVelocity`（老执行器最兼容）；选了 `Align` 但执行器造不出新约束时**自动降级**回 `BodyVelocity`，飞行不会再"不生效"。
- 👁 **观战恢复**（3.7.2 误删）：传送页「玩家列表」**右键某个玩家 = 观战他**；顶部新增「停止观战」按钮。
- 🧱 **穿墙防回弹**：穿墙时朝某方向移动，会沿该方向给温和推力帮你挤过服务端碰撞；不动时不推。

**反陷阱免伤（修 2 个真 bug + 1 个新能力）**
- 修「开反陷阱自带加速」——原来它每帧把 `WalkSpeed` 无条件写成 `16×倍率`，跟加速叠加成 2 倍速。
- 修「被撞误判」——开加速时速度高，会被自己判成"被撞"→每帧清零水平速度。
- 修「回拉=瞬移」——原来回拉每帧最多 40 格，本身就是瞬移量级。
- ➕ **新增「无视触发」**：开关打开就把角色所有部件 `CanTouch`/`CanQuery` 关掉，陷阱的 Touched/射线**根本收不到你**，直接不触发（而不是触发后再抢救）。

**鼠标**
- 修「关 UI 后鼠标锁死在中心」。
- 修「关 UI 后鼠标隐形」。

**藏地下（⚠️ 用户已确认接受风险）**
- 关闭改回**单帧浮回**（一步回地面，最快）。⚠️ 有触发 `Illegal Teleport 267` 被踢的风险。

---

## v3.8.1 · 2026-09-17

### 🧹 事件连接清理审计（4 处修复，**无功能变化**）

一次针对"事件连接生命周期"的全量审计：把 **75 处 `:Connect`** 逐条判定"卸载后能不能断开"，
其中 3 处**确实没有断开路径**，全部修掉。所有修复都是"登记 + 显式关闭"，不改任何功能行为。

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 1 | 翻译模块的玩家名监听 | `Players.PlayerAdded:Connect` 是**裸连接**。`Players` 服务永不销毁 → 卸载 / 热重载后它会一直留着，**每重载一次叠一条** | 用 `T(...)` 登记进 `SYS.Conns`，卸载时统一断开 |
| 2 | ProximityPrompt 变更钩子 | 两类问题：①两条连接从未登记（卸载后残留）②用**实例属性 `__TransHook`** 做"只挂一次"标记 —— 属性挂在**世界里的 prompt 实例**上会**跨脚本代次残留**，重载后新代码看到属性=true 就跳过挂钩子，而旧钩子已断 → **提示文本的"实时跟随翻译"静默失效** | 改用**本代次私有的弱表** `PromptHooked`（`__mode="k"`，对象销毁自动出表）+ 两条连接登记进 `SYS.Conns` |
| 3 | 反陷阱（位移/弹开免疫） | 三条连接（角色 `Touched` / `CharacterAdded` / `RenderStepped`）**没登记**，且卸载清单里**漏了** `CleanTrapGuard` → 开着它卸载会残留，每次重载**再叠一条永久每帧回调** | 三条连接登记进 `SYS.Conns` + 卸载时显式调 `CleanTrapGuard()`（双保险） |
| 4 | 同上的复核加固 | 复核时发现 #2 的"已钩"标记**先于连接置位** —— 万一某次 `Connect` 抛错，该 prompt 会被记成"已钩"却没连接，之后**永久不再重试** | 标记改为"两条都挂上之后"才置位；单条成功也照样登记（不会漏清理） |

> 口径说明：**UI 上的按钮/悬停连接**（约 20 处）没有登记，但它们的父 `ScreenGui` 在卸载时会被 `Destroy`，
> 连接随实例自动断开 —— 判定为**可接受，不改**。
> **角色子对象连接**（`charConns`）同样判定为**不作登记**：它靠"重绑时断开 + 角色销毁自动断"已足够，
> 而 `SYS.Conns` 是**只增不减**的表，给它加 `T()` 反而会在每次重生后长期累积死连接。
> （这条是审计里提出的 P3 建议，评估后**主动不采纳**并写明理由。）

### 🔍 审计过程与独立复核

- **静态分析**（`luau-analyze`）：全量 1515 条诊断**均为 W0 警告**，0 错误。
  其中 879 条 `TypeError` 全是"未知全局"（分析器不认识 Roblox / 执行器全局），已**独立交叉核对**：
  出现的 38 个名字 **100% 落在项目的 39 项白名单内**，无未声明名缺陷。
  另有 4 条 `UnknownType`（注解引用了 Luau 标准库没有的 `Instance`/`EnumItem`）—— 无运行时影响，保留。
- **连接清点**：75 处 `:Connect` 逐条给出"注销路径"依据；结论 75/75 具备断开路径。
- **RemoteEvent 安全**：本脚本是**纯客户端**脚本，不存在 `RemoteEvent:OnServerEvent`（服务端 API）；
  `OnClientEvent` **仅用于只读接收**（4 处）；ProximityPrompt 只做**只读**属性变更监听；未使用 `BindableEvent`。
  库内自建的 `FireServer`/`InvokeServer` 调用属**既有自动化功能（用户点击触发）**，本次未改动。
- **危险 API**：`os.execute` / `io.*` **0 处**；`loadstring`/`load` 仅 2 处，都用于**官方自更新**路径
  （从本项目仓库拉取后执行）；`HttpGet` 仅用于版本检查与更新，且都先判能力再调用。

### ✅ 验证

七步门禁 **11/11 全过**；整脚本仿真 **239 通过 / 0 失败**、v69 探针 **275 / 0**、**被 pcall 吞掉的错误 0 条**；
词法作用域检查新增未声明名字 **0**；构建等价性通过。
`check.py` 更新/新增 3 条回归锁（Prompt 钩子改用弱表、玩家名监听必须登记、反陷阱登记 + 卸载清理），
防止这几处又退回裸 `:Connect`。

---

## v3.8.0 · 2026-09-17

### 🎯 自动选人顺序改了默认值：**正在瞄我的 → 我指定的目标 → 最近的敌人 → 屏幕中心**

**你想要的是这个顺序**（之前「我指定的目标」永远排第 1，把其它全压住了）：

| | 顺序 |
|---|---|
| 改前（v3.7.x） | ①我指定的目标 ②最近的敌人 ③**正在瞄我的** ④屏幕中心 |
| **改后（本版）** | ①**正在瞄我的** ②我指定的目标 ③最近的敌人 ④屏幕中心 |

人话：**谁正拿准星对着你，就先打谁**；没人瞄你时，才轮到你在战斗页指定的那个人；
再没有，就打最近的；最后才看屏幕中心那个。

- 「优先模式」下拉框第 1 项（默认）的文案同步改成 `正在瞄我的→指定→最近→屏幕中心`
- **其它 4 个优先模式（最近→屏幕中心 / 准星指向 / 血量最低 / 屏幕中心→最近）行为不变** ——
  它们的「指定目标」仍排在最前
- ⚠️ **一个例外**：打开 **「🚫 只打指定目标」**（严格模式）时，指定目标**仍然绝对最优先**，整条链不参与。
  那个开关的语义就是"只打他一个"，让他排在"正在瞄你的人"后面是自相矛盾的 —— 所以没动它。

### 🧪 仿真台加/改了 3 条用例（专门钉死这个顺序）

| 用例 | 查什么 |
|---|---|
| ④-4（**期望值反转**） | Enemy 更近、Enemy2 正瞄着我 → 必须选 **Enemy2**（旧断言要求选近的 Enemy，已按新顺序改写） |
| ④-5（新增） | **同时**存在"正瞄我的 Enemy"和"指定的 Enemy2" → 必须选 **Enemy**（指定目标让位） |
| ④-6（新增） | 打开严格模式后同样的局面 → 必须仍选 **Enemy2**（严格模式不被削弱） |

结果：整脚本仿真 **239 通过 / 0 失败**（237 + 2 新增）、v69 探针 **275 / 0**；七步门禁 **11/11 全过**。

### 🔍 顺手加了一行诊断（方便你自己确认）

战斗页「立即测试一次」的输出里多了一行 **`选人顺序: ...`**，直接把你当前生效的顺序用中文打出来，
例如 `正在瞄我的 → 我指定的 → 最近的 → 屏幕中心`；开着严格模式时会在后面注明
`★「只打指定目标」开着: 实际只打指定的那个人, 整条链不参与`。

> 这行与真实选人用的是**同一份计算**（`chainOrder`），不是各写一遍 —— 避免"界面说的"和"实际做的"变成两回事
> （v111 那次就是漏改了提示文案，后来靠回归锁才钉住）。

### 🧰 静态检查同步

`check.py` 回归锁更新/新增：默认链断言改为 `{"aiming","near","center"}`；新增两条骨架锁
（①「指定目标」的插入位由 `chainOrder` 计算且只写一份；②严格模式的短路口必须在），
防止以后有人改回"指定目标永远第 1"而界面文案看不出来。

### 🔢 版本号说明

这次改的是**默认行为**（不是修 bug），所以按项目口径走 **次 +1（3.7.2 → 3.8.0，补丁归零）**。
如果你希望它只算个补丁号，告诉我一声，next 一次改小东西时并进去即可。

---

## v3.7.2 · 2026-09-17

### 🧹 历史遗留死代码清理（**无功能变化**）

一次"全面体检 + 清扫"：把长期迭代里**已经没人调用、却一直留在文件里**的死开关 / 死配置 / 死函数 /
死字段全部删掉，并顺手修掉两处**会误导阅读**的命名问题。**本版不新增、也不改变任何功能**，纯粹卸包袱。

- 源码：**372619 → 367817 字节**（−4.8 KB / −1.3%）；**7626 → 7516 行**（−110 行）
- 发布版：**228949 字节 / 6416 行**
- 行为等价：七步门禁 **11/11 全过**；整脚本仿真 **237 通过 / 0 失败**；v69 探针 **273 / 0**

#### 删掉的东西（每一处都先经"零读取"扫描确认，改前断言原文）

| 类别 | 具体 |
|---|---|
| 死开关 | `ESPBox`（v71 透视方框的遗留附加项；透视主体早已是人物高亮 Highlight） |
| 死配置项 | `GymMode`、`CB_Priority`、`CB_SilentMode`（旧版单选框/静默模式；选人早已改由**优先链**决定） |
| 死函数 | `SYS.Spectate` / `SYS.StopSpectate`（观战）、`espParent`（+其局部 `ESPParent`/`ESPPMsg`）、`SYS.PanicHide`（紧急隐藏，**没有任何按键或按钮绑定它**）、`inWorld` |
| 死字段 | `SYS.KickOrig`、`SYS.Device`、`SYS.FloatBtn`、`GENV.CheatMenuExtras`、`GENV.CheatUnloaded` |
| 只写不读的状态位 | 战斗：`CB.LockedAt` / `inOwnRay` / `KeyAcc` / `IsEnemyBase` / `MovingNow` / `ScanHz` / `ERR`；翻译：`Trans.CacheMax` / `cachePath` / `cacheDirty`(+`markCacheDirty`) / `saveCacheNow` / `clearCacheSimple` |
| 残留局部变量 | `an` / `hum` / `maxR` / `cbStroke` / `DeepHideCF`(4 处) / `FLM`(3 处) |
| 命名隐患 | 战斗渲染循环 `local function tick(dt)` → 改名 `cbRenderTick`。原来它会**遮蔽 Roblox 全局 `tick`**，而同一文件另一处的 `tick()` 调的其实是全局函数 —— 读代码极易搞错 |

#### ⚠️ 差点误删的一个（值得记一笔）

`CB.BodyOf` 险些被当成死成员删掉。原因：它在本文件里**字面只出现 1 次**（就是赋值那行），
而真正的使用处写的是**别名**：

```lua
local cb = SYS.Combat          -- 这里起了别名
...
local root = (cb.BodyOf and cb.BodyOf(SYS.LP.Character)) or nil   -- 用别名读的
```

字面搜 `CB.BodyOf` 当然找不到 `cb.BodyOf`。
**处置**：恢复该行并在旁边加注释标注；同时给扫描工具加 **namespace 别名解析**
（`local X = SYS.Combat` 这类别名会一并计入引用），从工具层面堵死"别名读取被误判成死代码"这一类坑。

#### 🧪 仿真台新增：老档案兼容用例（第 ⑥ 段 · 3 条断言）

本版**删掉了 3 个配置键**（`GymMode`/`CB_Priority`/`CB_SilentMode`）和 1 个开关（`ESPBox`），
于是用户机器上**旧版本存下的 `CheatMenuV491_Config.json` 里必然还带着这些陌生键**。必须确认升级后：

1. 读档**不会复活**这些已删除的键（靠 `SYS.C_[k]~=nil` 守卫丢弃）；
2. 有效键照常恢复；
3. **整段不抛错**（不能被 `P()` 的 pcall 悄悄吞掉 —— 上上版那个"加载配置没生效"就是这个死法）。

> 踩的坑：造"老文件"**必须用仿真台的 `HS:JSONEncode` 生成**，手写 JSON 字面量会被桩的假解码器
> 变成 nil，用例就"空过"了（假通过比不测更危险）。

#### 🧰 静态检查同步

- `check.py` 第 8 段回归锁跟着改：`DeepHideCF` 锁改为"**不得出现**"；设备检测锁由原来钉 `SYS.Device=DEV`
  改为钉 `DEV.small` + `ViewportDisplaySize`（只写不读的字段删了，本地表 `DEV` 保留）。
- 词法作用域检查（`check_globals.py`）：新增未声明名字 **0**。

---

## v3.7.1 · 2026-09-17

### 🐞 修复：「📂 加载配置」其实一次都没生效过

**症状**：在设置页存了一份命名配置，再点「加载该名字的配置」——**界面上什么反应都没有**，
开关不会变、也不弹"已加载配置"的提示。功能像是死的。

**根因**（一行代码）：

```lua
-- [02] 配置持久化段，SYS.LoadProfile 里
for _,def in ipairs(UI.Defs or {}) do P(SYS.EnsurePage,def.name) end
```

`UI` 是在 **[13] UI 工具段**才用 `local UI={}` 声明的局部变量；而这段代码在 **[02] 段**，
位置在它前面 —— Lua 按词法解析，这里的 `UI` 只能被当成**全局变量 `UI`**，而它恒为 `nil`。
所以每次读档都在这一行抛 `attempt to index a nil value (global 'UI')`。

要命的是**这一行在"应用开关"之前**：异常一抛，后面的
「先把所有开关统一关 → 再按存档里的状态逐个开 → 刷新界面显示」**整段都没执行**。
于是：

| | 实际结果 |
|---|---|
| 开关标志位 `SYS.T_` | **被改了**（看起来是开的） |
| 功能本体（onChange） | **从没调用** → 飞行/透视/加速一个都没起来 |
| 界面开关的显示 | **不刷新**（所以用户"什么都看不到"） |
| 「已加载配置」提示 | 不弹 |

**修法**：改用 `SYS.UI` —— 就是 [13] 段注册的同一张表（`SYS.UI=UI`），并加判空：

```lua
if SYS.EnsurePage and SYS.UI then
    for _,def in ipairs(SYS.UI.Defs or {}) do P(SYS.EnsurePage,def.name) end
end
```

**为什么这么久没被发现**：这个异常被上层的 `P()`（pcall）吞掉了，控制台里什么都不留；
而它又只在"点加载配置"时触发，不是启动路径。**是新增的仿真用例把它抓出来的**（见下）。

### 🧪 仿真台补了两轮新切角（这才是抓出上面那条 bug 的原因）

原有的用例已经覆盖"逐个开关开/关 + 效果断言 + 生命周期 + 幂等 + 边界"，
但有两块一直是空的：

| 新用例 | 查什么 | 结果 |
|---|---|---|
| **v125 开关交叉干扰 / 同资源抢占** | 46 个开关全开状态下【逐个关闭】，要求只影响它自己：不能连带关掉别的开关、不能连带注销别的功能的循环；全开↔全关往返 3 轮，要求无错误、无实体残留（高亮/速度体/朝向体/飞行附件）、属性还原 | 全绿 |
| **v126 配置存档 / 读档往返** | 存档 → 全关 → 读档：8 个开关要全部回到开启，**且效果真的恢复**；反向用例：档里没开的开关必须被关掉（不能残留当前状态）；两个档案互不覆盖 | **抓出上面那条 bug** |

> 顺带修了两个**测试基础设施自身的失真**（这类问题会让用例"假通过"，比没测更危险）：
> 1. **JSON 桩假序列化**：`JSONEncode` 原来只把表的**引用**塞进一个全局变量，`JSONDecode` 再原样还回来
>    —— 于是"存了再读"拿到的是**同一个表**，而且所有档案共用一份数据。改成"每次编码发唯一 blob 号 +
>    解码返回深层拷贝"，"读回来的是一份独立数据"这件事才真的被验证。
> 2. **断言被环境满足**：读档用例原来写的是 `高亮数 > 0`，而仿真场景里**游戏自带 3 个高亮**
>    —— 于是"读档整段没执行"也能通过。改成和**全关基线**比大小。

### 🛡 新增 2 条静态回归锁（`check.py` 第 8 段）

- **[13] 段之前的代码不得出现裸 `UI.` / `CY.` 引用** —— 这类写法会被解析成全局 nil，
  运行时抛错，而抛错点常常正好卡在"应用功能"之前，表现为"开关是开的、功能没起来"。
- **读档必须用 `SYS.UI` 且判空** —— 直接钉死这次修的写法。

---

## v3.7.0 · 2026-09-17

### 🔢 版本号改成三段式（新增补丁位）

> 你提的："加个补丁版本号，例如 3.7.0 / 3.7.1，用来修 bug 或者功能修复，不需要新功能或大优化的时候用。"

| 改动类型 | 号怎么走 | 例子 | 怎么发 |
|---|---|---|---|
| **新功能 / 大改动 / 大优化** | **次 +1，补丁归零** | 3.7.1 → **3.8.0** | 双击 `一键更新并推送.bat` |
| **修 bug / 功能修复 / 小补漏** | **只加最后一位** | 3.7.0 → **3.7.1** | 双击 `一键修复发行.bat` |

- 新增 **`一键修复发行.bat`** = 补丁版发行入口（底下就是给发行脚本加 `CM_VER_KIND=patch`）。
  功能版还走原来那个 `一键更新并推送.bat`，行为不变。
- 命令行等价写法：`python .workbuddy/build/push_now.py --patch`
  （本地路 `local_sync.py --patch` 同样支持；两边共用同一个版本脚本，保证升的是同一个号）。
- **旧的 `3.6` 这种两段号照样认**：一律当补丁=0 处理（`3.7` 与 `3.7.0` 是同一版，不会误更新）。
  升级后的第一版就会写成三段式。
- 「次 到 10 进位」的老规则保留：`3.9 → 3.10 → 4.0.0`。
- 游戏内的版本比较也同步支持三段（权重 主×1000000 + 次×1000 + 补丁）；
  **畸形版本号（`3.7.0.1`、CDN 错误页）一律不认**，当"没有新版"。

### 🚀 重新加载时先检查新版本（新增 · 设置页开关，默认开）

> 你提的："**自动更新，但不是已经注入之后在里面更新** —— 是重新加载的那一下先检查一遍有没有新脚本，
> 有就更新再打开，没有就正常打开。"

**两条自更新职能分开了**（以前只有一条，容易混）：

| | 管的是什么时候 | 行为 |
|---|---|---|
| 「🔁 有新版本时自动热重载」(v71) | 脚本**正跑着**的时候发现新版 | 存配置 → 卸旧实例 → 拉新版 → 当场热重载 |
| 「🚀 重新加载时先检查新版本」(本版) | 脚本**刚被加载**的那一下 | **有新版就直接用新版启动**；没有就正常打开 |

**为什么这样更舒服**：检查发生在**界面建立之前** —— 所以你**永远不会先看到旧版界面再被换掉**。
启动完会弹一条 `🆕 已更新 3.6 → 3.7.0`，告诉你这次换了版。而旧版代码在这时**直接结束**，
不会留半个界面、也不会两个实例抢相机。

**顺带解决的几件事**：
- **本地版也能查更新了**：本地自用版原来是「零网络」的（更新地址故意留空），于是本机那份最容易过期的
  文件反而永远查不到新版。现在内置了一份仓库基址兜底 —— 地址为空时自动用它。离线拉不到就安静跳过。
- **国内网络也能查到**：候选源按 **raw → ghproxy（反代 raw，内容新且国内可达）→ jsDelivr** 依次尝试。
  jsDelivr 放最后是因为它**分支缓存约 12h**，刚推的新版可能还没同步（会让"检查"看起来没生效）。
- **绝不误更新**：远端版本号必须是 `主.次` 或 `主.次.补丁` 格式才算数 —— CDN 错误页/限流页会被**跳过并换下一个源**。
- **绝不挡路**：取不到版本号、源码拉不到、新版编译不过、新版启动报错 —— 四种失败全部**回落到当前版本正常打开**，
  并且**不会留下"已更新"的假提示**。
- **开发版不自我更新**：源码里还是 `{{CM_VER}}` 占位符（未发行状态）时直接跳过，
  免得本地正在改的开发版一加载就被线上版顶掉。
- 设置页新增开关 **「🚀 重新加载时先检查新版本」**（默认开），想彻底不检查可以关掉。

---

## v3.6 · 2026-09-17

### ⚡ 加速执行器换代（官方推荐版）
- 默认从**已弃用的 `BodyVelocity` / `BodyGyro`** 换成 **`Attachment` + `LinearVelocity`（Vector 模式）**，
  与飞行那次保持同一套做法。旧的 `BodyVelocity` 保留为兼容选项，随时可切回。
- 「加速模式」选项改为 **`Linear`（默认）/ `BodyVelocity`（兼容）/ `WalkSpeed`**。
- **补上风险提示**：服务端按 *「每 tick 位移 > 正常速度 ×2」* 判定加速作弊，
  所以**倍率建议 ≤ 2**，调太高会被记一笔（这是公开反作弊实现的口径）。
- 关闭时新执行器（Attachment + LinearVelocity）**一并销毁**，不在角色上留残留；角色重生会自动重建。

> 顺带核掉一项：之前列的「隐身补 `LocalTransparencyModifier`」——**代码里早就有了**（隐藏/还原都在写），
> 那一条是我记错，实际不需要改。

---

## v3.5 · 2026-09-17

### 🛡 反陷阱免伤（新增 · 移动页开关，默认关）
> 地图上的道具 / 生物 / 陷阱造成的攻击与控制，以及**被滚石撞**的场景。

**完全豁免的部分**（客户端拥有自己角色的物理，每帧抢回就有效）：

| 免疫项 | 做法 |
|---|---|
| **被撞 / 被弹开 / 被推走** | 被撞后【水平速度清零（竖直保留，不影响下落跳跃）】+ 位置被拉走就**渐进压回撞击点** |
| **定身（走不动）** | 游戏把 `WalkSpeed` 清零/调小 → 每帧写回 |
| **布娃娃倒地** | 被改成 `Physics` / `PlatformStand` → 抢回 `Running`（与上帝模式同开时互相让位，不打架） |
| **被坐骑锁住** | 每帧 `Sit=false` 起身 |
| **被焊接钉住** | 断开**非角色自带**的 `Weld/WeldConstraint/Motor6D`（自带关节的 Part0/Part1 必含 HRP，不会误伤） |
| **客户端结算的伤害** | 监听血量，掉了**立刻补满** |

**触发判定**：钉在角色 `HumanoidRootPart` 的 `Touched`（谁撞我）+ **速度异常判定**
（水平速度 > 正常走路速度 ×1.6 也算被撞）—— 因为有些陷阱压根不触发 `Touched`（纯物理推动 / 区域判定）。

**受击后的位移逻辑 = 不反弹、不位移**：把速度清零并把位置**渐进压回撞击时的锚点**。
为什么不做"反弹"：反弹会产生**新的位移**，服务端看到的仍是异常位移；压回锚点才是
"服务端眼里你基本原地没动"，这才是真正的**不被推开**。回拉每帧上限 40 格，避免一次性瞬移触发 267 Illegal Teleport 踢人。

### ⚠️ 免不了的部分（必须说清）
**服务端结算的伤害，以及服务端强制的人形状态** —— 服务端是权威，它复制下来的血量/状态会盖掉客户端的写入。
所以这个开关的**实际强度取决于"本局的伤害和状态是谁结算的"**：休闲图多数判定在客户端 → 基本等于无敌；
有服务端反作弊的竞技图 → 只能免掉位移和状态类，血量该掉还是掉。

---

## v3.4 · 2026-09-17

### 🛡 反检测（基础版）—— 用户要求"高级做不到就把普通版做进去"
游戏自己的**本地反作弊**最常见的两招：①遍历 `PlayerGui` 找可疑 GUI ②本地脚本直接踢人。这版做了两件**不需要高级 hook 也能做**的事：

- **UI 藏进隐藏容器**：菜单和浮动按钮不再挂在 `PlayerGui`，改为按
  **`gethui()` → `CoreGui` → `PlayerGui`** 的优先级自动选择（有执行器的"保护"接口就顺手打上）。
  这样**游戏的普通脚本遍历 PlayerGui 时扫不到我们的界面**。
- **拦"本地踢人"**：执行器提供 `hookfunction` 时，把本地 `Player:Kick` 挂起 → 本地脚本踢不动你。
- ⚠️ **必须说清的边界**：这两条只降低被**本地**反作弊发现的概率，**不是隐身**；
  **服务端反作弊完全不受影响，服务端踢人也拦不住**（那是服务端断连，客户端改不了任何东西）。
  启动日志会打印 `菜单挂载: gethui/CoreGui/PlayerGui · 防踢: 已启用/不可用`，一眼能看出这台执行器支持到哪一步。

### ✈️ 飞行执行器重写（参考公开实现 Archon / Ziffixture）
- 默认执行器从**已弃用的 `BodyVelocity/BodyGyro`** 换成 **`LinearVelocity` + `AlignOrientation`**（官方推荐）。
- **不再把 `Workspace.Gravity` 清零** —— 那只是客户端本地改重力，服务端照样施加重力，两边不一致就会**被位置校正拉回**（这就是"开飞行被拉回"的经典原因）。
- **接管 Humanoid 状态机**（禁 `Running` + `ChangeState(PlatformStanding)`），不再让角色控制器跟你的速度"抢方向盘"。
- 「飞行模式」新增 `Align`（默认）/ `BodyVelocity`（老执行器兼容）/ `CFrame`（**标注为会被拉回、不推荐**）。

### 🚀 传送改分步插值
- 远距离传送不再**一帧写到底**（那等于"单帧不可能位移"，会被服务端拉回）。现在按**每步 ≤60 格分帧推进**，最后一步精确落点；近距离仍一步到位，不影响手感。

### 🌐 翻译：数值单位掩码补漏
- 原本只掩码到**双字母**单位（`1Qa`），这类大数字游戏的**3~4 字母组合单位**（`1UDc`/`1QaDc`）会漏给模型、被翻成"1亿1千万"。已补齐，且 `2nd`/`3rd`/`x2`/`20%` **不误伤**。

### 📱 手机 / 平板（v3.3 已推，此处一并记录）
缩放改为**只缩不放**（保证整块塞进屏幕）、拖动**支持触屏**、新增**收起/展开**按钮、新增**常驻浮动按钮 ☰**（触屏没有键盘也能开菜单）。

---

## v3.3 · 2026-09-17

### 🔔 新版本提示 —— 让还在用旧版本的人也能收到推送
- **每次启动都会检查一次新版本**。以前关掉「自动检查更新」就**完全不检查**了 → 你会一直沉默地用旧版，
  根本不知道有新功能。现在无论开关怎么设，**检查照做、提示照给**。
- 检查到新版本会弹消息，**消息里直接带新旧版本号**：`🆕 发现新版本 3.2 → 3.3`。
- 设置页的开关改名为 **「🔁 有新版本时自动热重载」**，语义变清晰：
  - **开着**：直接热重载到新版（保存配置 → 干净卸载 → 加载新版 → 自动恢复开关）。
  - **关掉**：**只提示、不自动升级**；想升级就点「⬆️ 检查更新并热重载」。
- 拿不到新版源码 / 新版编译不过时，**保留当前版本**并提示失败原因，不会把自己搞坏。

### 📱 手机 / 平板适配修复（同一版本号内追加，未升号）
> 你实测反馈：**菜单太大、拖不动、很多按钮点不到、手机上根本打不开**。四条全部定位到具体代码并修掉。

| 你的反馈 | 根因 | 修法 |
|---|---|---|
| **太大 / 按钮点不到** | 缩放上限写的是 `小屏 ? 1.25 : 1`（**小屏还放大**），下限抬到 `0.75` —— 两条加起来会让**菜单比屏幕还宽**，右侧/底部按钮永远在屏幕外 | **上限恒为 1（只缩不放）**，用实际视口算出的比例保证**整块菜单都塞进屏幕**；下限降到 0.30 只在极端小屏兜底 |
| **没法拖动** | 拖动只监听 `MouseButton1` / `MouseMovement`，**触屏没有鼠标事件** | 统一接受 `MouseButton1 / MouseMovement / Touch` → 按住标题栏即可拖 |
| **手机上打不开** | 开关菜单绑的是 **G / 右Shift**，触屏没有键盘 | 新增**常驻浮动按钮**（圆角 ☰）：**轻点=开关菜单，按住拖动=挪位置**（拖动>8 像素才算拖，避免误触） |

**另加「收起 / 展开」**（你点名要的）：顶部栏新增 **`▬`（收起）/ `▣`（展开）** 按钮 ——
收起后只留一条标题栏、其余控件全部隐藏，再点即展开；收起状态下依然可以拖动。

> 与「关闭(✕)」按钮不重叠（收起键在它左边 14 像素处）。
> ⚠️ 触屏手感（松动/收起高度/浮动按钮大小）请**实机再确认一轮**，不满意我下一版继续调。

---

## v3.2 · 2026-09-17

### 🔧 文案与行为对齐（v3.1 的收尾修正）
- v3.1 把默认优先链改成了「**最近→攻击我的→屏幕中心**」，但「⚡ 一键开战」的提示气泡和它的说明文字
  **还在说旧顺序**（`攻击我的→最近→屏幕中心`）。这版把这两处也改过来，**UI 文案与实际行为现在一致**。
- `check.py` 新增 2 条回归锁：旧顺序文案不得再出现 / 新顺序文案必须在位 ——
  防止以后再改链时又漏改某处文案（这种漏改静态检查是看不出来的）。

---

## v3.1 · 2026-09-17

### 🎯 自动选人顺序改成你要的顺序
**新的默认顺序：① 我指定的目标 → ② 最近的敌人 → ③ 正在打我的 → ④ 屏幕中心**

- 「指定目标」永远排第一 —— 在战斗页设了指定目标就一直打他，压过所有自动规则。
- 旧默认链是「正在打我的 → 最近 → 屏幕中心」，现在把**最近的**提到最前面。
  UI 里「优先模式」第 1 项同步改名为「**最近→攻击我的→屏幕中心**」，其余 4 个模式没动。
- `CB_PrioMode` 存的是数字，**老配置不用改**，行为直接按新默认走。

### 🛡 无敌盾 / 击杀解锁 / 重生重锁 —— 逐项实测确认
这版把离线仿真台补齐了游戏真实的死亡/复活事件结构（`ReplicatedStorage.Remote.*`），
第一次能把整条链路跑通并断言（新增 14 项，全过）：

| 你问的 | 实测结论 |
|---|---|
| **从击杀到死亡要多久** | **零额外延迟**。靠游戏广播的死亡事件（`GameService.Killed` / `EntityService.Died` / `Any.TouchDead` / `Any.Suicide`），事件到的那一帧立刻解锁，**不等下一轮扫描** |
| **死后会不会重生** | 重生与否由**游戏服**决定，脚本不参与；但它监听 `Respawn` / `Revive` / `Spawned` 三种复活事件，**任一到达就放回候选**（三种都实测通过） |
| **不重生时尸体停在哪 / 会不会一直被锁** | 尸体停在**游戏自己放的位置**，脚本不动它。血量归零 / 没有 Humanoid / 只留尸体 三种情况**都不会被锁**（含 8 秒 TTL 自愈之后仍不锁） |
| **指定目标优先是否已生效** | **已生效**：更远的 Enemy2 被指定后压过更近的 Enemy 被选中，并被真正瞄准（夹角 **0.000°**）。指定目标死亡后**自动回退**到优先链，不会卡死 |
| **扳机：击杀即解锁、重生才重锁** | **已实现**：击杀瞬间 `Target`/`TargetPart` 同时清空（不等扫描）；重生前跑再多轮扫描都不会重新锁；只有重生事件到达后才重新锁定 |

### ⚠️ 一个残余边界（实测记录，非本次能修的）
若某游戏**从不发死亡事件**、且**血量不归零只倒地**，在「🛡 只锁活人」**关掉**时，
8 秒 TTL 过期后尸体可能被重新选中。**保持「只锁活人」开着**（默认开）即可避免。

---

## v3.0 · 2026-09-17

### 📱 手机 / 平板自适应（v108）
> 以前菜单只按 PC 屏幕设计：手机上被缩成一小块、按钮小到点不准。

- **设备检测**：`UserInputService.TouchEnabled`（纯触屏＝手机/平板）+ `ViewportDisplaySize`（Small/Medium/Large）。
  结果存进 `SYS.Device`，启动时打印一行 `[CheatMenu] 设备: 触屏=… 纯触屏=… 视口档=… 小屏适配=…`（诊断页可查）。
- **SafeArea**：算缩放时把 `GuiService:GetGuiInset()` 的顶部栏／刘海高度计入边距（手机横屏尤其明显），
  避免菜单被刘海或顶部栏压住。
- **触摸友好缩放**：

  | 项 | PC | 手机 / 平板 |
  |---|---|---|
  | 缩放下限 | 0.55 | **0.75**（太小点不准）|
  | 缩放上限 | 1.00 | **1.25**（小屏字小，允许放大）|
  | 边距 pad | 40 | **28**（屏幕本来就小，少留白）|

- 与旧行为的差别：原来 `math.min(fit,1)` **只缩不放**，且下限固定 0.55；现在触摸设备会主动放大到看得清。

---

## v2.10 · 2026-09-17

### 🎯 无敌盾改为「完全跳过」（等护盾结束再打）
- **旧行为**：带无敌盾的敌人只是**降权排到最后** —— 只要别人都不可选（比如全服都在出生保护期），
  脚本仍会**锁着他不放**：打不掉血、还一直占着扳机和准星。
- **新行为**：带 `ForceField` 的目标 **完全不进候选池**，谁都不锁，等护盾自然结束就恢复锁定。
- 新开关：战斗页「🛡 跳过无敌盾 (带盾的不打, 等护盾结束)」，**默认开**（想回旧行为就关掉它）。

### ✅ 锁敌链路逐项确认（仿真台新增 7 个用例，全部通过）
| 项 | 结论 |
|---|---|
| **击杀→判定死亡** | 走**游戏事件**（`GameService.Killed` / `EntityService.Died` / `Any.TouchDead` / `Any.Suicide`），事件一到即判死，**不等 Humanoid.Health 归零**（自适应那些"血量不归零只倒地"的游戏）|
| **击杀后是否持续锁定** | **立刻解除** —— 死亡事件回调里直接 `CB.Target=nil`，不用等下一轮扫描 |
| **死亡后会不会被再选** | **不会**。`CB.DeadAt` 让 `alive()` 判死；TTL=8 秒自愈（防收不到复活事件时永久判死）|
| **重生后** | 复活事件（`Respawn`/`Revive`/`Spawned`）清掉 `DeadAt` → **可以重新锁定** ✅ |
| **优先顺序** | **① 指定目标 → ② 正在瞄我的人 → ③ 最近的 → ④ 屏幕中心**。指定目标在 `pickTarget` 最前面单独处理，**压过所有自动规则**（仿真已断言）|
| **只锁活人** | 开着时"没有 Humanoid 的尸体"一律不算活人 ✅ |
| **只打视野内** | 隔墙的（含指定目标）都会被挡掉 ✅ |

> 关于"**从击杀到死亡多久**"：这个取决于**游戏服务端**（由它广播 `Died`/`Killed` 事件），
> 脚本不参与判定、只做监听 —— 事件到的那一帧就解锁，所以是"零额外延迟"。
> 如果某游戏不发这些事件，脚本会退化成看 `Humanoid.Health`（下一轮扫描内生效，约 0.03 秒）。

---

## v2.9 · 2026-09-17

### 🔧 修复：配置管理只存了参数、没存"开关状态"
- **现象**：保存一份"战斗档"（开了自动瞄准 + 自动开火），加载回来**开关不会打开**，只有滑条/选项回来了。
- **修法**：`SaveProfile` 现在连 **开关状态一起存**（档里多一个 `__T_` 段）；
  `LoadProfile` 恢复后用**脚本启动时同一套机制**（`SYS.SwitchOnChange`）把开关**真正应用起来** ——
  先统一关、再按档开启，避免"上一份档开着、这份档关着"的功能残留。
- **注意**：自动配置（CFG，开机自动生效那份）**仍然不存开关**，保持"每次启动默认全关"的既定行为；
  只有**手动存档**才存开关。

---

## v2.8 · 2026-09-17

### ⚡ 性能
- **翻译扫描间隔 1.0s → 0.5s**（新文本最多 0.5 秒内被翻译）；无新文本时等待 2s → 1s。
- **服务端参数加大**：`-c 12288 -np 24 -b 4096 -ub 1024 --cache-reuse 256 -sps 0.2`
  （原 `-c 8192 -np 20 -b 2048 -ub 512`）。

### 📊 实测数据（RTX 3070 8GB，同机）
| 场景 | 吞吐 |
|---|---|
| 单条串行 | 55 ~ 61 tok/s |
| **16 条并发**（界面批量翻译的真实场景）| **101 ~ 125 tok/s，0.8~1.0 秒翻完 16 条** |
| 长文本（162 / 196 字）| 无截断，译文完整准确 |

- 显存占用 **5644 MiB**（给游戏留 ~2.5GB，边玩边翻不会 OOM）。

### ⚠️ 参数坑（别再踩）
- **每槽上下文下限 = 512**（llama.cpp 硬限制）：想用 `-c 12288 -np 32`（每槽 384）腾并发，
  会被引擎自动提升回 `16384`（32×512）—— 日志原文
  `n_ctx is not divisible by n_seq_max - rounding down to 16384` / `n_ctx_slot = 512`。
- **所以"降上下文换并发"不成立**：要并发就必须加 `-c`（加显存）。
- `--cache-reuse 256` + `-sps 0.2`：复用 system prompt 前缀缓存（翻译场景同一条 prompt 反复用）。
- **最优平衡点 = `-c 12288 -np 24`**（24 槽 × 512）：并发够用，且游戏可共存。
  若**只翻译不玩游戏**，可上 `-c 16384 -np 32`（显存 5956 MiB）。

---

## v2.7 · 2026-09-17

### 🔧 修复：「人死了倒地还在锁尸体」
- **根因**：`hookDeathEvents()` 里查 `ReplicatedStorage.Remote` **只查一次**。脚本加载经常**早于**
  游戏创建这套结构（本游戏是 `ReplicatedStorage.Remote.*`，运行时才挂载），于是 7 个死亡/复活
  事件**一个都没订上** → `CB.DeadAt` 永远为空 → 只能靠 Humanoid 猜 → **人死了还锁尸体位置并开火**。
- **修法**：`hookDeathEvents(attempt)` 带**重试**（找不到 Remote 就每 2 秒重试，最多 80 秒），
  订阅加 `CB.DeathHookedSet` **去重**，一个都没订上就继续重试。
- **怎么确认修好了**：战斗页「一键诊断」里会显示 `死亡事件订阅: N 个`。
  **N > 0 才算订上**；若一直为 0，说明这游戏不发这些事件，需要换信号源（告诉我）。

### 🔧 修复（v2.6 已含，一并说明）
- **只锁活人失效**：死亡信号提到判断最前（游戏报过死就直接判死，不看 Humanoid 还在不在）；
  「只锁活人」的判死**提前到**"游戏高亮容器"旁证之前（高亮是延迟清理的，会把尸体放行）。
- **优先模式不起作用**：原实现是"每环排序后直接返回"，导致**第一环吃掉一切**、后面的
  「最近 / 屏幕中心」永远走不到；改为**逐级真筛选**。
- **隔墙仍锁人**：隔墙射线缓存 0.25s → **0.1s**（目标缩回墙里后不再被判成"可见"）。
- **鼠标**：关掉菜单后恢复的是"**打开菜单前**"的鼠标状态（原来用脚本加载时的旧值 → 关掉菜单鼠标还悬着）；
  自由视角下打开菜单时，自由视角不再与菜单抢鼠标。

### ✨ 新增
- **配置管理（多套存档）**：设置页可保存 / 加载 / 删除多份命名配置（`CheatMenu_Profile_<名字>.json`），
  自动配置不受影响。
- **全局搜索**：侧栏底部搜索框，输入即实时过滤**当前页**控件。
- **热键可自定义**：设置页「热键设置」，点按钮 → 按新键就绑定（菜单 / 切换目标 / 鼠标传送）。
  右 Shift 始终可以开菜单（备用）。
- **加载器镜像源 3 → 8**：`raw` → jsDelivr(主/Fastly/Gcore) → githack → gitmirror → ghproxy → ghfast。

### 📦 文件
- `CheatMenu.lua`（发行版，走网络加载）
- `loader.lua`（放执行器里的 35 行加载器，8 源自动回退）
- `version.txt`（版本号）
- `CHANGELOG.md`（本文件）

---

## v2.6 · 2026-09-16
- 战斗三修（只锁活人 / 优先链 / 隔墙）+ 鼠标两修（关闭菜单恢复鼠标 / 自由视角不抢鼠标）。
- 加载器探针改为**动态源个数**（加减镜像源不用再改探针）。

## v2.5 · 2026-09-16
- 早期版本（战斗选人链、ESP 高亮、藏地下隐身 v2、翻译模块 v70 等）。

---

## 怎么同步（换账号 / 新环境）
1. **最快**：执行器里点『CheatMenu网络』（或跑 `loader.lua`）→ 自动拉最新版并加载。
2. **本地（不联网）**：双击项目里的 `一键更新并推送.bat` → 会写进执行器并推送仓库。
3. **版本核对**：菜单标题栏右侧显示版本号，与 `version.txt` 一致就是最新。

## 版本号规则
语义化：`1.0 → 1.1 → … → 1.10 → 2.0`；**只有源码 sha 变了才升号**（同一份源码重复构建不升）。
