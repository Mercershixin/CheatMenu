# CHANGELOG

> 只记**当前世代（V2）**起的变更。更早的历史在 git 里（`git log`）—— **不再在文件里堆**。

## 12.1.6 · 2026-09-25 —— 核心功能复查 + 函数/监听检查 + 符号·死代码·幽灵清理

用户：「透视 飞行 加速 挂机 重生 这些全部功能呢 都复查，然后函数 事件监听 检查一下修复，
符号 死代码 幽灵项目清理删除」。

### 再补两把尺子（原有 21 个审计答不了这两个问题）
- **`_audit_feature_chain.py`** —— 开关的**效果链路对称性**：能开 · 有反应 · **关得掉**。
  `_audit_buttons` 只验「点了不报错」，`_audit_switch_wiring` 只验「有读无写」一个方向
  ⇒「拨下去关不掉」这类问题**原来没有任何检查覆盖**。
- **`_audit_listeners.py`** —— 监听面生命周期：订阅有没有卸载路径 · 循环 on/off 配对 ·
  5 个 V3 监听面开关的 `on==false` 分支。
- 两把尺子都带**完整性自检**（解析出的调用数必须等于源码里的实际数量），避免「漏检的检查假装全绿」。

### 复查结论（点名功能逐条对账）
- **UI.Switch 81/81 链路闭合**（含「关得掉」判据）· **204 个控件接线完整** · **T_ 105 / 死键 0 / 残留 0**
- **订阅前缀 14 个全有卸载路径** · **V3 监听面 5/5 有 off 分支**（关掉会摘信号）· Connect 无丢弃
- 循环里唯一「只有 on 没有 off」的 `ACSample` 是**启动即采样的内部采样器**（卸载统一兜底），非用户开关
- **重生**：`SYS.GC()` 每次调用都重校验 `cChar/cHum/cRoot` 的 `.Parent`，
  13 处 `CharacterAdded` 分别重绑；`CharacterRemoving` 清缓存与子连接 ⇒ 换命/换服不会留旧角色引用

### 清理的三件
- 🔴 **`SYS.SetAntiFling`（防甩飞）—— 全项目唯一零引用函数，删**。AGENTS §3 曾把它列为
  「用户明确说还需要，别再删」，但它**既没有界面入口、`AntiFling` 也没在 `T_` 表里声明**，
  连启动恢复（遍历 `SYS.SwitchOnChange`）都带不动它 ⇒ 实际从来用不到。
  用户 2026-09-25 明确拍板「直接删掉」⇒ 连同 `FLING_MAX` / `SYS._flingN` 一起移除
  （后两者经 grep 确认只在该函数体内出现）。
- 🧹 **`SYS.C_.HitDist=30` —— 声明后全文件从未被读过的死配置键**（12.1.4 删「命中标记整簇」时漏网）。
- 👻 **4 处幽灵文案**：提示语指向**已不存在的控件** ——「一键修复」/「开始采样」×2 /「输出战斗诊断」。
  脚本把全项目 **247 个 UI 标题**枚举出来逐一比对过，一个都对不上。

### 顺手修（工具链，不占版本号的逻辑）
- **`_audit_c_.py` 的判据原来建立在【原文】上** ⇒ **说明文字里的 `HitDist=30` 被当成一条真声明**，
  于是删掉它之后仍旧报「声明了但代码从没用过 (1 个): HitDist」—— 一个纯注释制造的假结论。
  已加 `mask_comments()`（等长空格替换，字符串内容保留、偏移不变）⇒ 现在 **76 声明 / 76 引用 / 未使用 0**。

### 幽灵项目清理（本地文件，全部进回收站可还原）
- `.workbuddy/test/` 27 个一次性修复/基准脚本 · `.workbuddy/memory/_append_*.py` 3 个一次性追加器
- `.workbuddy/build/_tmp/` 3 份旧版本 `*.precleanup` / `*.predeadcode` 备份 · 1 份 9.8.1 期旧体检报告
- 共 35 个文件 / 2.83 MB。**保留** `.workbuddy/sim/`（`DISABLED.md` 写明「只作历史存档保留」）
  与 `.workbuddy/cloud/`（OCALE 云端执行，属允许的三种验证手段之一）

### 验证
`luau-compile` 双模式 exit 0 · 去注释复核（`AntiFling`/`FLING_MAX`/`_flingN`/`HitDist`/4 处文案）代码中出现 **0** 次 ·
`verify_all.py` **13/13** · `_audit_deadcode` **零引用函数 0** · `_audit_c_` 未使用键 **0**

## 12.1.5 · 2026-09-25 —— 全面检查：迭代代码 / 重复补丁 / 全部按钮可用性（新增两把尺子）

用户：「全面检查代码 看看是否进行迭代代码 和清理重复且无效的补丁修复，查看全部功能按钮 能不能达到正常的使用效果」。

### 先造了两把尺子（项目原有的界面审计是单行正则，系统性漏检）
- **`.workbuddy/build/_audit_buttons.py`** —— 界面控件全覆盖对账。★ **括号配对取完整调用**（跨行都认）：
  原有 `_audit_features*.py` 用单行正则，而本仓 `UI.Switch(` 有 82 个、单行正则只匹配到 **20** 个
  ⇒ 它们报的"界面无控件 25 个 / 找不到实现 11 个"**全是漏检假报**。
- **`.workbuddy/build/_audit_patches.py`** —— 迭代代码 / 重复补丁 / 无效修复体检（5 类判据）。

### ✅ 全部功能按钮：**204 / 204 接线完整**
10 页 · 按钮 71 · 开关 81 · 滑块 33 · 下拉 19。六类判据**全 0**：
回调引用的符号不存在 0 · 开关无 onChange 且键没人读 0 · 滑块/下拉指向未声明 `C_` 键 0 ·
空回调按钮 0 · 控件引用未声明 `T_`/`C_` 键 0 · 同键被多控件注册 0。
⚠ 静态只证明「接线是对的」；**"按下后在游戏里真有没有效果"必须真机验**（本机无 Roblox）。

### 🔴 修的 2 处硬问题
- **一枚从未生效过的补丁（迁移条件自相矛盾）**：
  `if (SYS.C_.CB_FireDelay or 0.08)==0.06 then …=0.08 end` —— 兜底值 0.08 与比较值 0.06 不一致，
  老存档没设过这条时**永远等不到 0.06** ⇒ 这一行**从写下那天起就没执行过**。
  本意 = 只把「等于旧默认 0.06」的升到 0.08（旁证：同文件读这条时的兜底正是 0.06）⇒ 兜底改成 0.06。
  （同段另外三条 SnapMaxAngle/SnapMinGap/SnapDelay 兜底与比较值都自洽，只有这条是手误。）
- **历史名残留（真·迭代痕迹）**：全项目只有**一个**菜单实现却叫 `CreateMenuLegacy`，
  而 `CreateMenu()` 只是一层 `return CreateMenuLegacy()` 的空壳 ⇒ 已正名为 `CreateMenu` 并删掉 wrapper。

### ✅ 逐类核过、确认干净的（不是没查）
**成员级重定义 0** · **默认值表串键 0** · 迁移块 6 处**全部有意义**（无一处检查已不存在的键）·
`SYS.SetLoop` 同名 21 个**全是 on/off 成对挂卸** · 同键"赋两次"15 处**全是互斥分支或先清再设** ·
复制的 6 行块只有 3 组（`getfenv` 小兜底，合并收益极低）· 裸名同名 13 个**全是不同作用域的同名局部函数（合法）**。

### 顺带修 2 处审计自身假报（工具链，不占版本号）
- `_audit_features2.py` F 段：它在 `s_noui`（把 `UI.Pages` 抹掉的那份）里找"实现"⇒ 界面区里定义的成员
  全被判成"找不到实现"（实测误报 11 个：ConsoleTee/SetNoAggro/Lab/SetCamGuard/CheckUpdate/UnloadAll…）。
  改成在**整份源码**里判存在性 ⇒ 现在 0 个；并在标题加了"控件级对账请看 `_audit_buttons.py`"的指引。
- `_audit_patches.py` ①⑤ 判据分层：裸名同名是合法的局部函数（不再当硬问题）；"重复块"要求块内有 ≥4 行真代码
  （否则 `end` 密集处必然假撞车）。

### 验证
`luau-compile` 双模式 exit 0 · `check.py` 0 · `_audit_buttons` 204/204 · `_audit_patches` 硬问题 0 · `verify_all` 13/13。

---

## 12.1.4 · 2026-09-25 —— 清理无效功能 / 无效声明 / 无效扫描（两轮 + 一层层收敛）

用户：「清理无效功能监听事件 清理无效的声明 避免在 ui 菜单占用位置」。

### 判据（不是"看着像没用的就删"）
只有**三条同时成立**才动：① 入口零引用（全文件没有任何地方调用它）
② 它绑的 `T_` 键**永远不可能为真**（没 UI、也没别的写入点）③ 对应 UI 行早在 11.8.5~11.8.6 按你要求删掉了。
⇒ 只要有一处还活着就保留。**逐处核引用面**后才改。

### 删掉了什么（净 −8477 B ≈ 200 行；函数 830 → 766）
| 类别 | 内容 |
|---|---|
| **已删功能的整簇后端**（UI 已删 + 入口零引用） | 蜘蛛感知模块(含 `SYS.SpiderSense`/`SetSpiderSense`，9.7 KB) · **hook 隐身**模块(11 KB) · **UI 文字脱敏**模块 · **反指纹自检** `Prot.SelfAudit`(7.2 KB) · **Attribute 回写**模块 · **firesignal 伪装**模块 · 自动打小游戏(`AutoHitScan`/`AutoHitTick`/`SetAutoHitMinigame`) · `SYS.SetDeadOn`+两个薄包装 · `SYS.SetDRHit`/`SYS.SetDRAim` · `SYS.SetRayNamecall` |
| **第 2 轮：删完入口后新暴露的孤儿** | 命中标记整簇(`SYS.HitMark`/`HitMarkHud`/`HitMarkTick`/`HitMarkClear`/`HitMarkDestroy` + `drGui`/`fireObj`/`_globalFn`/`_hitParentGui`/`SYS.InterKey`/`_hitLabel`/`HITMK_MAX`/`HITMK_SEC`) · `DR.ShowHit`/`drHookOne` · `rnd` · scanner 辅助 `dictN` |
| **无效声明** | `T_` 键 6 个（`SpiderSense`/`DeadOn_Freeze`/`DeadOn_NoBlow`/`DeadRails_HitMark`/`DeadRails_AimProbe`/`AutoHitMinigame`）⇒ **T_ 111 → 105**；孤儿导出 `SYS._doorCut/_doorSoft/_doorFake`（只写不读）；**14 个 `luau-analyze` 点名的未用 local**；悬挂引用（卸载清单 3 行 · UIMask 死引用 3 行 · `AC.List` 的 D14 项 + `d14` 函数 · 模块索引 2 行） |
| **无效扫描** | 删掉 2 个扫描器：`🕷 蜘蛛感知扫描` · `⏱ 死亡倒计时扫描`（对应功能已不可启用，永远只会报"已扫描: false"）⇒ 扫描器 19 → 18 |
| **UI 占位文案**（**不新增任何 UI**，只清残留说明） | 「防甩飞」bullet（功能 UI 已删）·「静默瞄准」bullet（功能已删，并把"这三条"改成"这两条"）·「MachineParty 页不再单独提供」的已删页引用 |

### ★ 没有删、而是**接回可用**的（6 个诊断）
`SYS.TimeCheck`（时间函数有没有被换）· `SYS.MetaCheck`（game 元表有没有被改）· `SYS.NetOwnerInfo`（网络所有权）·
`SYS.FpsGcInfo` · `SYS.Diag.ACFramework`（反作弊框架识别）· `SYS.CurKick`（反挂机倒计时）
—— 它们原先**只由被删的「反指纹自检」调用**，删掉可惜（都是真有用的只读体检）。
⇒ 新注册一个扫描器 **「🩺 一致性 / 只读体检」**把它们接回来。
**扫描是控制台输出、不占 UI 任何位置** —— 正好满足「避免在 ui 菜单占用位置」。
（并在其中写清"动态注入式反作弊下 ACBlock 要谨慎开"的判读提示。）

### ⛔ 明确保留（有保护，不动）
`SYS.SetAntiFling`（AGENTS §3「用户明确说还需要」）· 4 个光照键 `FullBright/NightVision/NightVisionPro/SuperLight`
（老存档迁移桥，删了会让老存档丢光照设置）· `SYS.DeadOnTime` / `SYS.DRCombat` 模块本体
（入口已删 ⇒ 已成惰性代码；整块精确切要 500+ 行且**无真机验证**，本轮只登记不切）。

### 验证
`luau-compile` 双模式 exit 0 · **被删的 30+ 个符号逐个复查：残留引用全为 0**（无悬挂）·
`check.py` 问题数 **0** · `_audit_switch_wiring`：**T_ 105 / 死键 0 / 间接驱动 11 / 残留 0**（原残留 5 ⇒ 归零）·
`_audit_features` 死开关 **0** · `_audit_features2` **零引用函数 19 → 1**（只剩受保护的 `SYS.SetAntiFling`）·
`_audit_luapitfalls` 0 · `_audit_implicit_globals` 0 · `verify_all` 13/13。
UI 控件/提示总数 **338 → 338（一个没加）**。

### 顺带修的工具链
`_audit_hygiene.py` §⑦ 的"开关键三向对账"把 `REMOVED_FEATURES` 块本身当成了"声明" ⇒ 误报
「10 个键声明里同时出现在 REMOVED（会互相打架）」。实测那 34 个键**没有一个**声明在 `T_` 里（各只出现 1 次 = 就在 REMOVED 块里）⇒ 假报，已修。

---

## 12.1.2 · 2026-09-25 —— 全工程通读 + 全面检查后修的 2 类源码问题（+ 5 处工具链缺陷）

用户：「先通读我的工程，告诉我目录结构、代码约定和 git 状态，然后进行全面的检查和修复逻辑/符号等问题」。

### 怎么查的（四路交叉，互相补盲）
① 项目总门禁 `verify_all.py`（13 步）· ② `luau-analyze` 原始诊断**逐类归类**
（2 621 条 —— `GlobalUsedAsLocal` **0** 条；1 454 条 `TypeError` 逐条过滤后**非噪音 0** 条）·
③ 项目 20 个 `_audit_*.py` · ④ 技能工具 `lua_review.py`（结果不可用，见下）。
⛔ 全程只用静态分析/单元测试/云端分发三种手段，**没有**搭 mock、**没有**整脚本仿真。

### 🔴 修的源码问题
- **`PerfProfile` 是死开关**：界面有控件、`onChange` 也写了，但 `SYS.T_.PerfProfile` **全代码没人读** ⇒
  **存档里是 true 的用户，重载后界面显示"开"、剖面具实际没开** —— 铁律"开关四点"缺第 4 点的典型形态。
  ⇒ 在 `SYS.V3Boot` 里**显式补挂一次**（`if SYS.T_.PerfProfile then K2.Guard("v3.prof",K2.Sched.Profile,true) end`）。
  验证：`_audit_features.py` 死开关 **1 → 0**。
- 🧹 删掉 12.0/12.1 我自己引入的 4 个**零引用内核 API**：`K.LogCats` · `BUS.Prop` · `BUS.Attr` ·
  `HK.RestoreOwner`（定义了从没被调用）。文件头补提醒：**内核只留真被调用的接口，加接口请同时接上调用点**。
  验证：`_audit_features2.py` 零引用函数 **17 → 14**（剩下 14 个全是"界面已删、后端保留"的待点头批次）。

### 🔧 修的工具链缺陷（都不占版本号，但每一个都在"让检查真的生效"）
- **4 个只读审计脚本默认路径写死成 `C:\Users\Administrator\Desktop\1.txt`（占位符）**：
  `_audit_implicit_globals.py` · `_audit_loops.py` · `_audit_thirdparty.py` · `_audit_ui_binding.py`
  ⇒ **一跑就 FileNotFoundError 崩** = 这几个检查**从来没生效过**。全改成走 `srcpath.src()`。
  ⚠ **崩掉的检查比没有检查更危险**（看着像"跑过了没事"）。
- **_audit_ui_binding.py 标注适用范围**：它认的是第三方 UI 库写法，本仓自研 UI 下恒为「共检查 0 个控件」，
  文件头写明"查本项目的开关接线请用 `_audit_switch_wiring.py`"。
- **_audit_loops.py / _audit_thirdparty.py 的循环判据按条件分档**：旧判据把一切没 wait 的 `while` 都标"★高危"，
  实测 2 条全是 `while #t>c do table.remove(t,1) end`（环形缓冲裁剪，**自己会停**）。
  ⇒ 现在只有 `while true`（或 `repeat…until false`）且体内无 wait/break/return 才叫高危。**必然假报 2 → 0**。

### 检查结论：代码本身是干净的
`verify_all` 13/13 · `check.py` 0 · `_audit_switch_wiring` **死键 0** · `_audit_c_` 0 · `_audit_errcheck` 0 ·
`_audit_luapitfalls` 0 · `_audit_implicit_globals` 0 · `_audit_hygiene` 无硬结论 · `_audit_syntax` 词法层干净 ·
`_audit_chunklocals` **76/200**（V3 内核整段包在 `do..end` 里，没吃 chunk 名额，余量充足）·
`_audit_members` 连接无丢弃 · `LocalUnused` 14 / `LocalShadow` 4 逐条核完**无功能缺失**（遮蔽全是有意）。

### ⚠ 记一条"工具不可信"（避免以后照着它改错代码）
技能工具 `lua_review.py` 在这份脚本上报 error 10 / warning 744，**逐条核验后全部是假报**：
行号整体错位（报 `L19443 drg 未声明`，实际那行是 `UI.Round(grip,12)…`；`L19627` 是注释行），
且把 `while … do task.wait(1.5) … end` 报成"没有 `task.wait`"。
⇒ **不要照它的行号改代码**；定位一律回原文核。（已同步写进该技能的已知局限）

---

## 12.1.1 · 2026-09-25 —— bug 全修 + 全功能测试（门禁 13/13）

用户：「bug 全部修复 功能全部进行测试」。

### 怎么做的
先跑 `.workbuddy/build/verify_all.py` 一把梭拿事实（12.1.0 = **11/13**，2 步红），
再把每条红**裁决成三类**（真 bug / 门禁过期 / 审计自身 bug），而不是照着报错盲改。
修完重跑：**13/13 步全部通过**。

### 修的真 bug
- 🟠 `AutoButtonColor=false` 少写 `btn.` ⇒ 赋到了全局，按钮悬停仍会自动变色（本意是关掉）。
- 🟠 **4 个裸全局**（`noclipThread` / `NoclipAtt` / `NoclipVel` / `InfiniteJumpConn`）：
  泄漏进 `getgenv()`（可检测指纹）、且同一执行器里重载会粘上一次的值。
  `check.py` 里本来就有锁要求 `local NoclipAtt,NoclipVel=false,false` ⇒ 是改动中**把声明弄丢了**。
- 🟠 **穿墙自建 `Attachment`+`LinearVelocity` 与飞行驱动互抢**：飞行开着时两个
  `LinearVelocity(MaxForce=math.huge)` 挂在同一个 root 上 ⇒ 速度抖 / 推不动。
  ⇒ 飞行期间收掉穿墙自己那份（AGENTS §2 硬规则④：[4] 层驱动实例只允许 `SYS._Move` 创建）。
- 🟡 **6 个数值配置"存了也读不回"**：`AtlasCap` / `CamZoom` / `GameNameCache` / `HitDist` /
  `LightMode` / `WL_Sel` 被读被写但没在 `C_` 表声明 —— `LoadConfig` 的还原分支带
  `SYS.C_[k]~=nil` 守卫 ⇒ **没声明的键存进去也永远读不回来**（用户改的值一重载就丢）。
  ⚠ `LightMode` 特意声明成空串 `""`：迁移条件正是 `==nil or ==""`，写成"关闭"会让**老存档的光照设置迁移失效**。

### 修的自查 bug（12.0/12.1 新写的代码）
- `W.Tags` 的连接只塞进 `SYS.Conns`、没进 BUS 注册表 ⇒ 「标签变动」**关不掉**、连接泄漏。
- 调度器 `SC.Del` 在 `Pump` 遍历中 `table.remove(SC.order)` ⇒ 当帧会**静默跳过后面的一个任务**。
  改为"只打死亡标记 + 下一帧头部压缩"。
- 空闲静默开着时再开功能 ⇒ 任务加进已挂起的调度器 = **开了没反应** ⇒ 开功能即自动退出静默（并同步开关状态）。
- ★ **实体 / 值对象 / 属性三个监听面只管"开开关那一刻"扫一遍**，之后新出现的实例永远漏掉
  —— 与「实体出现 · 不用轮询」的承诺不符 ⇒ 补 `DescendantAdded` 动态接入。
- `EventAtlas` / `LazyRebind` 两个开关**没接 onChange** ⇒ 拨了没反应；`LazyRebind` 现在真生效
  （关掉后连 `DescendantAdded` 那次重绑也一起停）。
- `EventAtlas` 文案说过头 ⇒ 改成"只管启动时全量走一遍"（图谱一直有增量维护）。

### 门禁修复（纯工具链，不占版本号）
- `check.py` 两条**过期**断言：①加速 WalkSpeed 写回的变量名 `spd`→`w`（先 `math.max(1,spd)` 夹住）；
  ②飞行 CFrame 模式在 11.5.0 起**就是有意**真写 `CFrame`（`FlyEnsure/SYS._FlyVel` 已按用户要求删除）
  ⇒ 断言改成锁"能自动降级 + 真写 CFrame"这个可观察行为。
- `_audit_hygiene.py` 两处自身 bug：①只去注释、**保留字符串内容** ⇒ 新增的扫描**说明文字**里出现
  `hookmetamethod(...)` 被数成真调用点（从此误报「hook 有挂无还原路径」）；
  ②只认 `restorefunction`，不认本项目"拿原函数再 hook 一次"的还原惯例。
  ⇒ 加 `mask_str()` + 按 `newcclosure` 判别安装/还原。
- `_audit_luapitfalls.py`：`print` 的裸全局改写是**有意为之**（常驻控制台镜像，卸载时 `ConsoleTee.restore()`）
  ⇒ 加 `ALLOW_ASSIGN_GLOBAL` 白名单并写明理由。
- **新增一条锁**：源码头部的「📌 当前源码版本 = X」必须 == `build/VERSION`
  （防"改了名忘了改注释"，以后一看源码就知道是哪版）。

### 验证
`luau-compile` 双模式 exit 0 · `check.py` 问题数 0 · `_audit_luapitfalls` 0 · `_audit_c_` 0 ·
`_audit_hygiene` 无硬结论项 · `_audit_errcheck` 0 · `_audit_switch_wiring` **死键 0** ·
6 个单元测试（真 `luau.exe`）弹道 822 / 富文本 45 / 扫描落盘 15 / 射击判据 97 / 通道风险 70 / 物件透视 7
= **1056 项 0 失败**。⛔ 真机行为未验（本机无 Roblox）⇒ 见 `事件库/2026-09-25_全功能测试矩阵_12.1.1.md`。

---

## 12.1.0 · 2026-09-25 —— 公开仓库技法调研 + 全部并进【一个扫描功能】

用户：「看他们怎么绕过飞行加速/回血/锁血/上帝/高亮/透视，和扫描事件功能，全部放在一个扫描功能里面」。

- **调研**（GitHub 近一个月，2026-08-25~09-25）：`DarkHUB`(08-27) / `SpongeHUB`(09-06) / `CISI-ACF`(08-29, **反作弊框架**)。
  · Fly = `BodyVelocity`+`BodyGyro`+`PlatformStand`（**自建实例起随机名** ← 唯一值得借鉴的一点）；
  · **两个 hub 都没有 godmode / 无限血 / 血锁实现** ⇒ 印证"服务端结算，客户端做不了"；
  · 高亮主流用 `Drawing API`（不是 `Instance` ⇒ 遍历 PlayerGui 抓不到，但没有表现层）；
  · ⛔ 别人的四招大量不抄：`__namecall` 总入口 hook（红线）· 扫全树 Destroy AC ·
    指纹擦除（删 `getgenv` 全局 / hook `loadstring`·`require` / 改元表 `__index`）· 写假 `Heartbeat` 属性。
- ★★ **最有价值的一条（来自反作弊框架）**：反作弊分两类 ——
  **A 静态扫描式**（④层中性名/脱敏有用）vs **B 动态注入 inspector 式**（随机间隔注入 + 限时回报 +
  `DependantValues` 数值比对 + 玩家/会话分组防扩散）。对 B 类：**拦通道/删 remote = 直接吃超时判罚**
  （所以 `ACBlock` 遇 B 类是**负收益**）· **改名/清全局完全无效** ·
  **唯一活路 = 让上报数值自洽（别伪造，只做本地表现）**。
- **落地**：把缺的 4 块**全部注册成 Scanner**（**不新增第二个扫描按钮**），
  扫描器 **15 → 19**：🛡 能力绕过判定（飞行加速/回血/锁血·防击倒/上帝/高亮透视/传送/反检测 逐条
  「判定·哪层·怎么做·做不到什么」，开头先给 `AuthorityMode`/网络所有权/`StreamingEnabled` 三条硬前提）·
  🧩 事件扫描（V3 图谱 7 类计数 + 已挂/挂起监听 + 5 个监听面 + 运行态/错误显形 + 最近下行）·
  🧪 自检合集（移动自检/高亮彻底性/穿墙射线/武器数值槽，原来散在 4 个按钮）·
  🕵 外部手法对照。全部**只读**（零上行、零改属性）。

---

## 12.0.0 · 2026-09-25 —— V3 内核（换内核，不重写功能体）

用户：「重新写一份新的架构的 lua 脚本，修复全部的 bug 和缺少的监听事件、逻辑、性能、绕过检测、欺骗」。

- **做法**：功能体 1.9 万行**一字未动**（铁律 §5：跨作用域搬代码 = "编译过、运行坏"），
  只换**单一入口** —— 改 `SetLoop`(55 处调用) / `SpawnLoop` / `OnRemote` / `SafeHook`(15 处)
  这 4 个函数的函数体，全脚本自动升级。
- **新增 `SYS.K` 内核八件套**：`Log`(分类环形日志) · `Guard`(**错误显形**，⛔ 不再静默吞错) ·
  `Sched`(**一个信号只开一条驱动连接**，任务按名字复用，开关拨动零连接变动) ·
  `Bus`(连接归属，可整组摘) · `Hook`(归属 + **后进先出**还原) ·
  `Net`(事件图谱 + 下行监听) · `Watch`(新监听面) · `Idle`(空闲静默)。旧 `SYS.*` 全部保留原名。
- 🔴 **修"缺少监听事件"的主因**：旧 `SYS.OnRemote` 在 remote 还没下发时直接 `return`
  ⇒ 监听**永久静默丢失**（`StreamingEnabled=true`、远程分帧下发都是常态）。
  改为 `K.Net.Bind`：**先挂起，下发后自动重绑**（`DescendantAdded` 立即重试 + 3 秒兜底任务）。
- 顺带修：`SpawnLoop` 出错只 `warn` 不进账 ⇒ 走 `K.Guard`；`UnhookAllSafe` 用 `pairs`（顺序靠运气）
  ⇒ 后进先出；`CharacterAdded` 时 `Humanoid` 常还没到 ⇒ 补"等 Humanoid"监听。
- **性能**：Heartbeat 驱动连接 **~29 条 → 1~2 条**；RenderStepped 3 → 1；
  图谱查询从每次 `GetDescendants` → 缓存 + `DescendantAdded` 增量。
- ➕ 新 UI 页「🧩 事件」（图谱 / 下行监听 / 5 个新增监听面 / 空闲静默 / 运行态）。

---

## 11.8.6 · 2026-09-24 —— 删干净剩余多行块 + 孤儿说明文字

用户：「1 那么没有对应功能的 说明也删掉」= 继续删 + 删掉已无功能的说明文字。

### 本轮删除（全部精确文本匹配，8/8 命中，编译 0 错误）
- `getreg 过滤` 开关本体 + 说明（多行）
- `firesignal 伪装` 的孤儿说明
- `hook 隐身` 开关本体 + 说明（多行）
- `UI 文字脱敏` 的孤儿说明
- `射线路 A` 的孤儿说明
- `防甩飞` 的孤儿说明
- `反指纹自检` Section + 自检按钮 + 总扫描层
- 保留：ACBlock 说明 · 「擦掉 GUI 可疑名」按钮（及其说明，因为它仍在）

### ★ 方法教训（今天第 5 次同族，这次做对了）
**多行块删除：只认"精确文本匹配"，绝不"数 end 自动删"**。
第一轮自动块匹配吞了 3000 行（已回滚）；本轮先 Read 拿到精确文本 → 精确删除 → 8/8 全中。

### 验收
`luau-compile` **0 错误**。

---

## 11.8.5 · 2026-09-24 —— 按用户要求删除 8 个"杂乱/低效"功能（UI 层）

用户列表：反甩飞 / 射线总入口 / 开火抖动 / getreg过滤 / Attribute回写 / UI文字脱敏 / 反指纹自检 / hook隐身。

### 本次删除（UI 层 + 接线，精确单行匹配，编译不过即回滚）
- 7 个 `T_` 默认键：`AntiFling` / `Prot_Stealth` / `Prot_RayNamecall` / `Prot_UImask` / `AttrGuard` / `StealthReg` / `FireSignalSpoof`
- UI 入口：防甩飞开关 · 射线总入口开关 · 开火抖动滑块 · Attribute回写开关 · UI文字脱敏开关 · firesignal伪装开关
- 总扫描层：`🫥 hook 隐身`；UnloadAll 清理：`SetAttrGuard(false)` / `SetFireSignalSpoof(false)`
- ⚠ 本轮**只删 UI/接线**（后端函数如 `SYS.SetAntiFling`/`SYS.SetStealth`/`SYS.Prot.SelfAudit`/`SYS.SetAttrGuard` 等仍在，
  但现在**已无入口调用 = 不可达**）。要连后端一起彻底删，下轮再做。

### 诚实说明
- 这些是"反检测类"，多数**有效但隐形**（不是没用，是"降低被抓概率"类）；用户要精简菜单，故按其要求删。
- 删除过程：第一轮"自动块匹配"在多行注册上跑飞（吞 3000 行）→ **自动回滚**；第二轮改用**精确单行匹配**成功。
  ★ **教训重申：多行块绝不能靠"数 end"自动删，只能精确文本删，或逐块删、删一块编一次。**

### 验收
`luau-compile` **0 错误**。

---

## 11.8.4 · 2026-09-24 —— 修「全亮」没全图亮：清 Atmosphere 雾 + 去暗色滤镜 + 环境光拉满

用户：「高亮全亮没有全图亮」。

### 根因
「全亮」之前只改 `Brightness / ClockTime / FogEnd / GlobalShadows`，**没动两个真正压暗全图的东西**：
- `Atmosphere`（雾/大气密度）—— Dead Rails 西部夜晚主题，几乎必有
- `ColorCorrectionEffect`（暗色/对比度滤镜）—— 夜景常用的"整体压暗"

⇒ 亮度再高，这俩一盖，远处/整体还是黑的。

### 改动（「全亮」模式补三样）
1. `LT.Ambient` / `LT.OutdoorAmbient` 拉满白 → 没有直射光的地方也亮。
2. `Atmosphere` 密度压到 0.001 → 清雾。
3. `ColorCorrectionEffect` 的 `Brightness/Contrast/Saturation` 归零 → 去暗色滤镜。

### 验收
`luau-compile` **0 错误**。

---

## 11.8.3 · 2026-09-24 —— 删除班卓琴功能（实测回血没成功）

用户实测：「回血没成功 删了吧」。

### 结论
- 班卓琴"自动弹奏"和"直接 FireServer 模板远程"的**伪装都没能让队友回血**（服务端校验了持有/结算）。
- ⇒ 按用户要求**整块删除**：本体函数 `SYS.SetBanjoAuto`、`T_.BanjoAuto` 默认值、功能页 UI、
  `UnloadAll` 清理 —— **4 处全部移除，残留 0**。
- ★ 这印证了之前的诚实判断：**Dead Rails 的治疗是服务端权威，客户端没有"凭空回血"的路**
  （唯一真实通道 = 真持有道具/乐器自己触发）。

### 验收
`luau-compile` **0 错误**；`grep BanjoAuto|班卓琴` = 0。

---

## 11.8.2 · 2026-09-24 —— 班卓琴：加「直接 FireServer 模板远程」伪装（赌服务端不校验持有）

用户：「看看能不能伪装一下，不使用物品、自己也不点，也能给队友回血」。

### 诚实结论
- **"不点"** ✅ 已做（11.8.0 自动弹奏）。
- **"不消耗道具"** ✅ 班卓琴是乐器本就不消耗。
- **"凭空给队友回血"** ❌ 治疗/血量是**服务端结算**，客户端伪装不了队友的真实血条。
- ⚠ **唯一能试的伪装**：治疗光环的触发远程 `banjo.PlayEvent` 在
  `ReplicatedStorage.Assets.Tools.Misc.banjo` 有**资源模板**（人人都有，不需要持有琴）。
  ⇒ **直接对模板远程 FireServer，赌服务端不校验"是否持有班卓琴"**：
  赌赢 = 没琴也能开治疗光环；赌输 = 服务端拒绝（无害）。

### 改动
`SYS.SetBanjoAuto` 里，除了"背包/身上的琴"，**新增兜底**：直接 `SYS.REvent("banjo.PlayEvent")` →
`FireServer`（即使没持有班卓琴也试一次）。

### 验收
`luau-compile` **0 错误**。

---

## 11.8.1 · 2026-09-24 —— 诊断：高亮"无效"的真相是【报错被吞】，已让报错显形

用户：「高亮（透视）坏了，自己排查全部功能」。

### 排查结论：高亮的整条链是完整的，不是重构删了事件
- `SYS.ESPTick()` 定义在 4961 行，由 `task.spawn` 循环每 0.15s 调一次（18444~18451）—— **都在**。
- ⇒ 问题不在"缺事件/断连接"，而在 **`P(SYS.ESPTick)` 把报错吞了**：ESPTick 里某一步一抛错，
  就"静默无效"，你看到的就是"高亮不出来"（和 11.7.7 扫描层吞错是同一族坑）。

### 改动（最小）
把 `P(SYS.ESPTick)` 改成 `pcall` 后**报错显形**（只提示一次）：
```
[高亮] ESPTick 报错(只提示一次): <真实原因>
        ↑ 把这行发我, 就能定位高亮为什么不出
```
⇒ 下次进游戏开高亮，若还不出，控制台会**直接告诉你原因**。

### 三个可能的原因（先预告）
1. **StreamingEnabled=true**（Dead Rails 实锤）⇒ 远处的物件没下发到客户端，**走近才出高亮**（不是 bug）。
2. 高亮开关没开 / 老存档把 `T_.ESP*` 记成了关。
3. ESPTick 里某步真抛错（现在会显形）。

### 验收
`luau-compile` **0 错误**。

---

## 11.8.0 · 2026-09-24 —— 新功能：🎵 班卓琴自动弹奏（Dead Rails 团队治疗光环）

> （注：11.7.10 版未写条目 —— 该版是工具链自动推进的，内容 = 11.7.9 无变化，仅版号推进。）

机制（从 Dead Rails 扫描实锤）：
```
[142] RemoteEvent  LocalPlayer.Backpack.Banjo.PlayEvent     ← 背包里就有弹奏事件
[146] RemoteEvent  ReplicatedStorage.Assets.Tools.Misc.banjo.PlayEvent
+ clientHealingAuraSystem + healingAura                      ← 弹奏触发【治疗光环】
```
⇒ **弹班卓琴 → 治疗光环 → 范围内队友回血**。★ **班卓琴是乐器不是消耗品 ⇒ 不消耗任何道具**。

**新开关（功能页顶部）`🎵 班卓琴自动弹奏`**：开启后每 3 秒自动 FireServer 一次 `PlayEvent` ⇒
**治疗光环常开，队友持续回血**；触发走**背包里那把琴** ⇒ **不需要装备在手上**。四点接线齐全。

**四个要求的如实回答**：① 自动给队友 ✅ ② 背包生效 ✅ ③ **秒回满 ❌**（光环是持续回血，速率服务端定，
只能"光环常开=最快回"）④ **也让自己 ❌**（给谁回是服务端逻辑，客户端改不了；先实测光环含不含自己）。

---

## 11.7.9 · 2026-09-24 —— 🔴 修：删 MachineParty 时**把 5 个页面从侧栏带走了**（用户截图抓到）

用户截图：**「我功能怎么就剩这么点了」**（侧栏只剩 战斗/视觉/移动/玩家 4 页）。

### 根因（又是一个"一行多项"）
`UI.Defs` 的页面清单是**两行**，每行**多个条目**：
```
14452: {name="战斗",…},{name="视觉",…},{name="移动",…},{name="玩家",…},
14453: {name="MachineParty",…},{name="传送",…},{name="挂机",…},{name="功能",…},{name="翻译",…},{name="设置",…},
```
我删"MachineParty 条目"时**把整行 14453 删了** ⇒ **传送/挂机/功能/翻译/设置 一起消失**
（页面定义本体都还在 —— 只是侧栏清单少了，所以**功能没丢，是入口没了**）。
★ 这和之前 `T_` 解析、层级标签是**同一族坑**：**"一行多项"的数据，绝不能按行删。**

### 修法
- 页面定义 **9 个全部在** ✓（只删了 MachineParty 的定义 ✓）
- 从 11.7.7 快照取回两行的**全部条目**，**去掉 MachineParty**，重建成 **9 项侧栏清单**：
  `战斗 ⚔ · 视觉 ◉ · 移动 ◈ · 玩家 👤 · 传送 ➲ · 挂机 ★ · 功能 ✱ · 翻译 🌐 · 设置 ⚙`
- `luau-compile` **0 错误**

### ⚠ 另一件事（如实记录）：11 个「看控制台」按钮**没删成**
- 两次自动删块都因**括号配对不严谨**导致 `luau-compile` 失败 ⇒ **安全网自动回滚**（源码始终没坏）。
- **根因**：按钮体里有 `if…then…end`，而我只数 `function`/`end` ⇒ 在 `if` 的 `end` 处**提前收尾** ⇒ 删半个块。
  **正解：数全部块开头**（`function`/`if…then`/`for…do`/`while…do`/`do`）对 `end`。
- ★ **但它们的内容已经全在总扫描里**（11.7.7 的 `📋 控制台输出` 常驻镜像会收**任何** `print`）
  ⇒ "统一入口"已达成，这些按钮只是**重复入口**，暂时保留不影响使用。

### 验收
`luau-compile` **0 错误**；侧栏 9 页全恢复（无 MachineParty）。

---

## 11.7.8 · 2026-09-24 —— 删除 MachineParty 整套界面（附：控制台按钮**未能安全自动删除**的如实记录）

用户：「删除 machineparty 界面里面的全部功能和这个界面，高亮自检控制台 这些乱七八糟的 需要控制台的
都说多少次了 放进总扫描里面，放不进的 就删掉这个功能和对应的描述介绍说明」。

### ✅ 已删：MachineParty 整页
- 删除 `UI.Pages["MachineParty"]=function(p) … end` **整页（88 行）** + `UI.Defs` 里的页面条目
  ⇒ 左侧页面列表里不再有 MachineParty。
- ⚠ 残留 `MachineParty` 字样 **3 处**（后端引用/注释），**未删**（删后端属另一类变更，先报告）。
- `luau-compile` **0 错误**；源码 19336 → **19247 行**。

### ⚠ 未能完成：11 个「看控制台」按钮的删除（**两次尝试都失败，已自动回滚**）
- 我写了个"括号配对"自动删块：**第一次太松**（吞了 40+ 行）；**第二次收严**（只认整行 `end)`）。
- **两次都 `luau-compile` 失败** ⇒ **安全网自动回滚**（源码始终没坏，这点已保证）。
- **根因**：那些按钮体里有 `if … then … end`，而我的**只数 `function`/`end`** ⇒ **在 `if` 的 `end` 处提前收尾**
  ⇒ 删掉半个块 ⇒ 语法坏。**正确做法是数全部块开头**（`function`/`if…then`/`for…do`/`while…do`/`do`）**对 `end`**。
- ⇒ **本版选择"宁可不删，也不删坏"**：按钮保留。
- ★ **但它们的内容其实已经全在总扫描里**：11.7.7 的 `📋 控制台输出`(常驻镜像) 会把**任何** `print` 收进去 ——
  **"统一入口"这个目的已经达到**；这些按钮只是**重复入口**。
- ⏳ 下次用**正确的块计数**逐块删（或按每个按钮的精确文本逐条删），**删一个编一次**。

### 验收
`luau-compile` **0 错误**。

---

## 11.7.7 · 2026-09-24 —— ✅ 真机确认 11.7.6 修好了 + 让扫描层**不再吞错误**

### ✅ 运行时确认（11.7.6 的修复生效）
用户用 11.7.6 重跑扫描 ⇒ **7 个新层全部注册上了**（层数 **15 → 22**）：
`📋 控制台输出` · `🔎 反指纹自检` · `📖 密码/提示文本` · `📡 服务端战斗数据` ·
`🧹 翻译·已跳过的动态文本` · `🧾 翻译·失败/退避清单` · `🚦 移动环境`
- ★ `📋 控制台输出` **已在工作**：抓到 300 条（含该服真实的 `Rojo_Id` / `Rojo_Target_PrimaryPart` 属性）。
- ⇒ **"把一大堆控制台收进总扫描"这件事，这次是真的成了**（上一版只是"代码在文件里"）。

### 本次修（11.7.7）
`🚦 移动环境` 层实测输出 `(移动自检执行失败)` —— **`SYS.MoveDiag()` 抛错了，但错误被吞掉**，看不到原因。
- **扫清吞错**：该层改为 `pcall` 后**把报错原文打出来**（`(移动自检报错 —— 把这行发我就能定位): ...`）。
  ⇒ 以后这类层报错**自带原因**，不用再猜。
- **加固 `MoveDiag`**：`WS.UseFixedSimulation` / `WS.StreamingEnabled` 等属性读取**全部走 pcall**
  （之前 `StreamingEnabled` 是**裸读**，一处抛错整层就废）。

### 验收
`luau-compile` **0 错误**。

---

## 11.7.6 · 2026-09-24 —— 🔴 修：新增的扫描层**运行时根本没注册上**（真机复查抓到的）

用户新版重跑扫描后我核对，发现**我加的层一个都没出现**；上一版我说"已并入总扫描"**是错的**。

### 现象（真机证据）
- 该扫描**确实是新版跑的**（含 11.7.4 的「框架式命名」诊断文本 ⇒ 版本够新）
- 但 `🚦 移动环境` / `📋 控制台输出` / `AuthorityMode` **全部 0 次**，层列表只到 `🧬 总扫描共享层`

### 根因（精确定位）
我的注册调用在 **1683~1770 行**，而 **`function SYS.RegisterScanner` 定义在 4201 行** ⇒
**注册跑在"注册函数还不存在"的时候**，`if SYS.RegisterScanner then` 这个守卫**直接静默跳过**。
⇒ 代码在文件里、`luau-compile` 也过，但**运行时什么也没注册**。

### 修法
1. **整块 88 行注册搬到最后一个扫描层（`🧬 总扫描共享层`）之后** —— 与其它扫描层同区。
2. 把"**注册时**判定 `SYS.Prot`/`SYS.Combat`/`Trans` 是否存在"改成"**调用时解析**"（同族 bug：
   注册时机太早 ⇒ 那时候这些符号也还没定义；顺带把 `Trans` 改走 `SYS.Trans`，避免作用域抓不到）。
3. ★★ **新增"运行时关键不变量"校验**：**所有 `SYS.RegisterScanner(...)` 调用必须位于
   `function SYS.RegisterScanner` 之后**。实测：**16 个注册点，早于定义的 = 0** ✅

### ★★ 教训（写进规范）
- **"代码在文件里" ≠ "运行时生效"**。凡是**注册/挂接**类改动，源码 grep **不算验收**，
  必须有**运行时证据**（真机输出 / 自检列表）。
- ⛔ **新增 `SYS.RegisterScanner` 一律加在扫描层定义的同一区域**，绝不要塞到文件里的其它位置。

### 验收
`luau-compile` **0 错误**；不变量校验通过（16/16 位置正确）。

---

## 11.7.5 · 2026-09-24 —— 常驻「控制台镜像」：一大堆控制台输出**一次全收进总扫描**

用户：「一大堆的那些控制台 放进统一扫描啊」。

### 为什么不再逐个改
上一版（11.7.3）用 `SYS.CaptureLines` 只并进了 **5 / 13** 个入口；剩下的 **8 个**（武器逻辑诊断 /
内部错误台账 / 怪·甩仇 / 穿墙自检 / 高亮自检 / 当前名单 / hook 实况 / 脚本清单）
处理函数都藏在**多行函数体**里，**按名字自动提取不可靠** ⇒ **逐个改 = 高风险**（多行 UI 块盲删就是搞坏文件）。

### 一劳永逸的做法：**常驻控制台镜像**
给全局 `print` 挂一层「**顺带记录**」的壳（**环形缓冲 300 行**），再注册成一个扫描层：
- 新层 **`📋 控制台输出 (常驻镜像 · 所有『打到控制台』的功能都收在这里)`** —— 显示累计条数 + 最后 60 条。
- ★★ **任何旧功能**（不管改没改过）只要 `print`，**自动出现在总扫描里** ⇒
  **不需要逐个改它们，也不需要 UI 里留一堆「看控制台」按钮**。
- 开销：每条 `print` 多一次 Lua 调用（print 本身低频）—— 可忽略。
- `UnloadAll` 已加 `print` 还原（`SYS.ConsoleTee.restore()`）。

### 验收
`luau-compile` **0 错误**。

---

## 11.7.4 · 2026-09-24 —— 功能搜索加「框架式命名」诊断 + 出《绕过方法总表》

用户给了一份**新服扫描**（`scan_116495829188952.txt`）并要求：「功能搜索补强一下，看看能不能正确透视可交互道具，
还有绕过飞行/加速/上帝/锁血等一切方法手段 都总结放进绕过功能里面」。

### 一、这份扫描的两个发现
1. **⚠ 它是旧版跑的** —— 里面**没有** 11.7.2 新加的「移动环境」层（`grep 移动环境` 为空、`AuthorityMode` 0 次）。
   ⇒ **执行器里加载的还是老脚本**（或该扫描早于同步）。**要看 SA 判定必须用 11.7.3 重跑一次。**
2. **该服是「框架式命名」** —— remote 全在 `ReplicatedStorage.JABBY_REMOTES.*`，
   名字是 `inspect_entity` / `replicate_query` / `update_entity` 这类**框架动词**，**零玩法语义** ⇒
   **别名搜索天生一条都匹配不上**（功能可用性总表全 ❌ 是"正确但没用"）。

### 二、本次补强
- **`功能可用性总表` 新增「框架式命名」诊断**：当 **≥80% 的类别搜不到**时，直接打印
  「这服是框架式命名，别名搜索天生无效」＋**替代路线**（交互类走 `ProximityPrompt`/`ClickDetector`；
  实体类看带 `Attribute` 的模型；真要发 remote 只能手动试参数）——**不再让用户对着满屏 ❌ 猜**。
- **《绕过方法总表》**（新增 `事件库/绕过方法总表.md`）：把**全部绕过手段**按"四层"归纳成一张表
  （客户端权威层 / 上行通道层 / 本地表现层 / 检测面层），每条注明**模块·开关·适用条件·做不到的**，
  并列出**红线**与**两条物理事实**，最后给"新服上手标准流程"。

### 三、透视可交互道具 —— **本来就支持**（实测）
`SYS.FindGrabbables` 就是专门找 `ProximityPrompt` / `ClickDetector` 的；全脚本 `ProximityPrompt` **25 处**、
`ClickDetector` **19 处**引用，且「🔍 物件透视」已把三者合并。该服扫描显示 **Prompt 51 + ClickDetector 82** ⇒
**交互物确实存在**。看不到时按此排查：① 开关没开；② 交互物在 Workspace 深层（需滚动/接近）；
③ 被 `StreamingEnabled` 剔除。

### 验收
`luau-compile` **0 错误**。

---

## 11.7.3 · 2026-09-24 —— 「看控制台」入口统一并入总扫描 + 删噪音播报

用户：「把那些打印 查看控制台 的什么东西的 都融合的融合 没用的就删除」。

### 先量（问题规模）
- **`print(` 调用点 219 处**（其中 7 处是"已开启/已关"状态的纯噪音）
- **散落的「点一下打到控制台」入口 13 个**：移动自检 / 反指纹自检 / 服务端战斗数据 /
  武器逻辑诊断 / 内部错误台账 / 密码提示 / 穿墙自检 / 高亮自检 / 怪·甩仇 / 翻译 dump×2 / 当前名单 / hook 实况

### 融合：新增 `SYS.CaptureLines` 桥（**关键设计**）
那 13 个入口都是**直接 `print`**、不"返回行"，所以**不能直接注册成扫描层**。
⇒ 加了 `SYS.CaptureLines(fn)`：**临时接管 `print` 收集输出**，调用完还原。
**这样不用重构任何旧 dump 函数**，就能让它们出现在总扫描里。

**新并入总扫描的 5 层**（总扫描 15 → **20 层**）：
- `🔎 反指纹自检 (UI 文字/实例名/执行器全局)`
- `📖 密码/提示文本`
- `📡 服务端战斗数据 (只读记录器)`
- `🧹 翻译·已跳过的动态文本`
- `🧾 翻译·失败/退避清单`

⇒ **以后所有"打到控制台"的东西，统一从「总扫描」一个入口看。**

### 删除
- 删掉 **2 处纯噪音状态播报**（`反陷阱免伤: 已开启…` / `防回退(过检测)已关`）
  ⚠ 只删"已开启/已关"这种纯播报；**诊断输出一律保留**（它们是上面那些层的实际内容，删了就没输出了）。

### 验收
`luau-compile` **0 错误**；总扫描层数实测。

---

## 11.7.2 · 2026-09-24 —— 修：移动环境检测**并入「总扫描」**（放错地方了）

用户指出：**「扫描那些东西 我都说了 放进总扫描哪里」** —— 11.7.1 我把 SA 判定加在了**移动页的独立按钮**里，
而它本该属于 **`SYS.Scanners` 那套「总扫描」（十二层）**。**这是我理解偏了，本次修正。**

### 改动
- 新增一扫描层 **`🚦 移动环境 (服务端权威 / 网络所有权 / 驱动 / 实际速度)`**，注册进 `SYS.RegisterScanner`。
- ★ **单一数据源**：该层直接调用 `SYS.MoveDiag()` ⇒ 与移动页「🧪 移动自检」**显示同一份内容**，不重复实现。
- 注册后**总扫描共 15 层**（新层排最前），点一次「总扫描」即可看到该服的移动环境判定：
  `Workspace.AuthorityMode`（Server ⇒ 移动类客户端无解）· 角色网络所有权 · 驱动实例 · 实际速度 · `UseFixedSimulation/StreamingEnabled`。
- 移动页那个按钮**保留**（1 次点击就能看，不必翻到总扫描），但**二者内容完全一致**。

### 验收
`luau-compile` **0 错误**；扫描层清单实测 15 条。

---

## 11.7.1 · 2026-09-24 —— 移动反作弊调研：给「移动自检」加 SA 判定 + 出调研档

用户问「移动反作弊能绕吗 / 看看别人 git 仓库怎么做」，并据此产出
**`事件库/移动反作弊绕过_公开技法调研与红线裁决.md`**。

### 核心事实
**Roblox 的 Server Authority 已于 2026-07-09 正式发布**（opt-in：`Workspace.AuthorityMode = Server`）。
开启后**引擎层直接拒绝**速度/飞行/穿墙/瞬移/属性篡改（服务端算 + 客户端预测/回滚）；
★ **并且 `SetNetworkOwner` 在 SA 下会直接报错** —— 「抢网络所有权」这条流传最广的老路**彻底死掉**。

### 本次改动（小而关键）
**「🧪 移动自检」新增一行：读 `Workspace.AuthorityMode`**，并直接给判读：
- `Server` ⇒ 打印 **⛔ 服务端权威已开，移动类客户端无解**（当场结案，别浪费时间）
- `Automatic` ⇒ 打印 **✅ 未开，飞不动是别的原因**（继续查"游戏每帧改写速度/服务端回滚"）
- 另附 `UseFixedSimulation` / `StreamingEnabled` 作为旁证。

### 调研结论（按能不能用排序 · 详见文档）
A 改上行位置上报（我们有 `AntiRevert`，**只对 TP 有意义**）· B 抢网络所有权（**SA 下报错**）·
**C 用游戏自己的合法机制（唯一在 SA 下可用的路）** · D 关反作弊模块（**无效**，只影响本地预测）·
E 蹭宽限窗口（**只能降低被标记概率，突破不了速度上限**）。

### 红线裁决
⛔ 不抄 `__namecall/__index` 关键词搜索 · ⛔ 不抄服务端伪造 · ⛔ 不加 `getrawmetatable/setreadonly`；
✅ 单点 hookfunction / 只读诊断 / 用合法机制 / 上报伪装做成可调开关。

### 验收
`luau-compile` **0 错误**。

---

## 11.7.0 · 2026-09-24 —— 过检测改为「默认开但只拦瞬移」+ 偷鱼(Steal a Fish) 别名补强

### 一、用户问「过检测可以默认开吗」→ **能，但必须换判据**
先讲清物理限制：`RevertCap` 是**上报速度上限**。
- **cap < 你的实际速度** ⇒ 服务端把你拽回 = **定在原地**（11.4.0 的原始事故）
- **cap ≥ 你的实际速度** ⇒ 它**等于空操作**（什么都不做）

⇒ 「持续限速」**不能默认开**；但「**只拦单帧大跳**」可以 ——
正常跑/飞每帧位移最多约 **10 格**（500 格/秒 ÷ 60fps），而 **TP 是一帧几百上千格**。
阈值设 **150 格/帧**，对正常移动**零影响**，只把瞬移拆成看不出来的连续位移。

**本次改动**
- `T_.AntiRevert`：**默认 false → true（默认开）**
- `C_.RevertCap`：**默认 60 → 0**（`0` = **不额外限速、只拦瞬移**，这是推荐值）
- 新增 `C_.RevertJump = 150`（单帧大跳阈值，格/帧）
- `SYS.SyncAntiRevert`：**不再要求"飞行/加速开着"** —— 它现在**开机即挂**
  （因为它真正管的是 TP 平滑，与飞/跑无关）
- UI 滑块文案改为「上报速率上限 (格/秒 · 0=只拦瞬移, 推荐)」

⚠ **诚实边界**：默认开 = 只做"TP 不显眼"。**想让它顺便把持续高速也伪装成正常速度，必须手动把 cap 调到小于你的实际速度** ——
那时代价就是会被服务端拽回。**两者不可兼得。**

### 二、偷鱼（PlaceId 99183404085821）能补强什么
结论（依据 11.6.x 的扫描诊断）：
- ✅ **能（已做）**：`claim` 别名补上该服**真实存在的 3 个领取通道**
  `FreeGiftRemotes.ClaimFreeGift` / `OfflineCashSystem.ClaimOfflineCash` / `FishIndexSystem.ClaimFishIndex`
  ＋ 关键词补 `freegift/offlinecash/fishindex`。⇒ 现有「一键领取」从此**稳定命中**（原本靠模糊匹配碰运气）。
- ⛔ **不能**：**移动类在这服无解** —— 无任何移动通道 + 服务端移动反作弊（`AntiCheatMovementGraceUntil`）
  + `ServerAuthority` 模块在位 ⇒ 客户端推不动。（诊断档见 `事件库/游戏/偷鱼(StealAFish)_移动限制诊断.md`）
- ⏭ **要"补强"就只能加新功能**（如自动放蛋 / 自动升池 / 被追鱼自动提速）——
  按项目铁律 **新增功能必须先报清单、经你点头**，本次**未加**。

### 验收
补丁 **9/9 锚点命中**；`luau-compile` **0 错误**。

---

## 11.6.6 · 2026-09-24 —— UI 简化第一步：76 个提示块折叠成「? 说明」

用户：「功能 设置 简化一下 不要太多功能」→ 选定方案 **「只合并 + 折叠（不删任何功能）」**。

### 先量（实测规模）
**202 个控件**（85 开关 / 39 滑块 / 15 下拉 / 63 按钮）分布在 **10 页**，外加 **47 小节 + 76 提示块** + 46 分隔。
最重两页：**战斗 50 个控件**、**功能 34 个**。★ 提示块比开关还多 —— 这才是"看着累"的主因。

### 这一刀：**只改 `UI.Tip` 一个函数**
- 由"**永远展开**的文本块"改成 **折叠式「? 说明」按钮**（点一下展开、再点收起）。
- ★ **76 处调用点一行没动** —— 一处改动覆盖全站。
- 效果：每页立刻短一大截（原本占满屏的说明变成 22px 一行按钮）。

### 验收
`luau-compile` **0 错误**；发行物缩小。

### 下一步（仍然"不删功能"）
**合并同类开关**（要改页面代码，分批做、每批单独发版）：
- 战斗页 25 个开关 → **瞄准类 8 个合并成「1 开关 + 1 模式下拉」**（底层 `T_` 键不变，只是 UI 收拢）
- 视觉页 10 个 ESP 分类开关 → **合并成 1 个下拉**
- 功能页 19 个 → 按"上帝 / 回血 / 传送 / 隐藏"分组收拢

---

## 11.6.5 · 2026-09-24 —— 清理收尾：修审计工具误判 + 删 8 处真残留（死键 4 → **0**）

用户：「继续吧」（= 同意做 P5 那三件清理）。

### ⚠⚠ 最重要：**那 4 个「光照死键」不是死键，差点被我误删**
`FullBright` / `NightVision` / `NightVisionPro` / `SuperLight` 被 `_audit_switch_wiring.py` 判成
「有读无写」，实际它们是 **`SYS.MigrateLightMode()` 里旧存档的迁移桥**：
```lua
SYS.T_.FullBright=(m=="全亮")        -- ← 这就是【写】
SYS.T_.SuperLight=(m=="超级光明")
SYS.T_.NightVision=(m=="夜视")
```
**删了会让老存档的光照设置丢失。** ⇒ **删键动作取消**，改为**修工具**。
- 根因：工具的"写判定"正则只认 RHS 是 `true|on|v|want|not not`，认不出 `(表达式)` 赋值。
- 修法：放宽为 **任何 `.Key = (非 `==`)` 都算写**。
- 修后复跑：**死键 4 → 0**，那 4 个键正确归入「间接驱动（11 个）」。
- ★ 教训：**工具会撒谎，删任何东西之前先看原始代码**（这次是先取证才没造成损失）。

### 删掉的 8 处真残留（都经"全文出现次数"证明无人读/无人调）
- **3 个 11.5.0 重写后的遗留接口**（各只出现 1 次 = 只有定义）：`SYS.FlyEnsure` · `SYS.FlyTeardown` · `SYS.FlyReleaseStates`
- **4 个 write-only 状态**（全文只出现 1 次）：`SYS._resumedFromUpdate` · `SYS._timeScale` · `SYS._timeAcc` · `SYS._timeLast`
- **1 个 write-only 连接句柄**：`SYS._SlDrop` ⇒ **只去掉赋值，保留 `T(...)` 登记**（否则监听会漏清理）
- 保留未动：`SYS._realTime`（出现 2 次，可能被读，不确定就不动）

### 验收
- 补丁式修改，逐行打印；`luau-compile` **0 错误**；源码 19,202 行。
- 四点对账：**死键 0 · 间接驱动 11 · 残留 0（全绿）**；`push_now.py` 的**基线已同步更新为 0/0**
  ⇒ 今后**任一 >0 立即告警**。

---

## 11.6.4 · 2026-09-24 —— P3/P4/P5 一次收口（结论：**大部分不是问题**）

新增 **`事件库/架构剩余工作总表.md`**（完整证据与裁决）。要点：

### P3 开关四点 —— **不需要"统一生成器"**
项目自带 `_audit_switch_wiring.py` 实测：`T_` **108** 个 → **有界面入口 97**、无入口 11
（**4 死键 + 7 间接驱动 + 0 残留**）。⇒ 手写接线本身是对的，重写成生成器是**零收益大 diff**；
真收益是**把对账工具纳入发版流程**。
⚠ **更正我上一轮的错判**：`FullBright` / `NightVision` / `NightVisionPro` / `SuperLight` **仍在 `T_` 里**
（当时我的 `T_` 区块解析器漏了"一行多键"）—— 它们才是那 4 个"有读无写"的死键。

### P4 玩法层隔离 —— **不做**
八层在文件里不连续，重排 = 跨作用域搬代码；而 **Luau 不会把"读未声明变量"报错**（静默变全局读）
⇒ 典型"编译能过、运行才坏"。**没有真机验证就不动。** 方案留档（先加 `Install/Uninstall` 壳 → 真机验 → 再搬）。

### P5 残留清理 —— 实测 37 个零引用 `SYS.*`，**逐类裁决**
- ✅ **删掉 6 个我自己引入的孤儿导出**：`SYS.ACL_N` · `SYS.Rnd` · `SYS.RootHum` · `SYS.FireSignal` ·
  `SYS.PosRebound` · `SYS.MoveUnmount`（只是把内部表对外挂一份，零引用，删了不影响功能）。
- ⚠ **待点头**：`SYS.FlyEnsure` / `SYS.FlyTeardown` / `SYS.FlyReleaseStates`（11.5.0 重写后已没人调用，
  属接口面变更，先报不动）。
- ⚠ **写了没人读**：`SYS._resumedFromUpdate` / `SYS._realTime` / `SYS._timeScale` / `SYS._timeAcc` /
  `SYS._timeLast` / `SYS._SlDrop`。
- ⛔ **刻意保留**：`SYS.Players/Stats/UI/Trans/CurKick/FindHUD/AFK/GymWeightNames…`（接口导出，别删）。
- ❌ **误报**：`_rands`（被 `SYS.N` 使用，我的正则把前导 `.` 排除了）。

### 验收
`luau-compile` **0 错误**（删完 6 行后重编）；源码 19,209 行。

---

## 11.6.3 · 2026-09-24 —— P2：分层补 [9] 编排 + 跨层依赖门禁（结论：没有真反向依赖）

### 做了什么
- 写了一个**作用域无关、结论可靠**的跨层依赖检查：只统计 `SYS.*` 引用（无歧义），按 P1 横幅判层，
  找出「低层引用了更高层 `SYS.*`」的反向依赖。
  （先试过「所有符号」版本 —— **误报太多、不可用**，已放弃：`apply` / `head` / `line` 这类词到处都出现，
  要真做必须写完整的 Lua 作用域分析。）
- 结果：81 处反向依赖里**绝大多数是启动器 / 卸载器 / 通知**（`UnloadAll` / `ToggleMenu` / `Notify` / `BtnRefs`…）
  —— 它们**天然跨层**。
- ⇒ 结论：**问题不在依赖，而在 P1 的标签缺一层**。已把 **2 个编排段单独标成 `[9] 编排（启动/卸载/收尾）`**，
  重建 57 行模块索引；`AGENTS.md §2` 同步加 `[9]`，并写明 **「[9] 是唯一允许「调用上层」的层」**。
- ★ 全程**仍只改注释**：自证「去注释后逐字符一致」= **521,254 字符未变**；`luau-compile` 0 错误；
  **发行产物 638455 B 不变**。

### 原计划的 P2（抽内核为「可注入模块」）—— 已放弃，理由如实记录
- 量过：4 个内核别名（`P` / `T` / `TT` / `DS`）**全文件只在第 463 行声明一次、零遮蔽**（共 705 个调用点）。
- 但**单文件单 chunk 里「可注入」没有意义**；把 705 处 `P(` → `SYS.P(` 是**零行为收益的大 diff**，
  还违背「接口不动」。⇒ 不做。**把分层做成可检查的门禁才是真收益**（本版已落地）。

---

## 11.6.2 · 2026-09-24 —— 修 P1 的层级标签（首个横幅判错）

- 11.6.1 引入的自动分层里，**第 1 段（文件头 + `T_`/`C_` 默认值 + 存档读写）被关键词频率误判成「[7] 玩法」**。
  已改为 **「[1] 配置」**。
- 其余 30 条横幅与索引统一补注 **「自动判定，段内可能混层」** —— 明确它是**导航提示**，不是精确边界。
- ★ **仍然只改注释**：自证「去注释后逐字符一致」通过，`luau-compile` 0 错误，**发行产物字节不变**。

---

## 11.6.1 · 2026-09-24 —— P1「只搬不改」：八层分层锚点 + 模块索引

> 承接 11.6.0 的 V2 重置。按 `AGENTS.md §2` 的八层，给源码插入**层级横幅**与**自动生成的模块索引**。
> ⛔ **只加注释行，逻辑零改动** —— 自证方式：词法去注释 + 归一化空白后逐字符比对，
> **新旧代码部分 521,254 字符完全一致**。

### 做了什么
- 源码按**顶层 `do` 块**切成 **31 段**，逐段判定所属层，段前插 3 行横幅；文件头之后插 **60 行模块索引**（层 → 段起始行）。
  **行数 19,030 → 19,218**（+188 行，**全部是注释**）。
- 分层结果：`[0]内核×8 · [1]配置×1 · [2]通信×3 · [3]检测面×3 · [4]移动×1 · [5]视觉×3 · [6]战斗×3 · [7]玩法×6 · [8]界面×3`。
- 门禁：`luau-compile` **0 错误**；等价性通过；加载器探针 6/0。
- ★ **发行产物实质未变**（构建本来就剥离全部注释）—— 与 11.6.0 的唯一差异仍是**内嵌版本串**。

### 为什么 P1 只"分层"不"重排"
当前八层在文件里**并不连续**（例：`检测面` 分散在 ~1760 / ~11900 / ~13400 三处）。
真重排 = **跨作用域搬代码**，会踩 `local` 可见性与上值顺序的坑；而 Luau 编译器**不会**把
"读了未声明的变量"报成错误（会静默变成全局读）⇒ 典型**"编译能过、运行才坏"**。
所以 P1 只做"看得见的分层 + 索引"，把真正的重排留给**有真机验证**的阶段。

### 下一步（逐层做、每层单独发版）
- **P2** 抽 `[0]内核` 为独立可注入模块（其余只改调用处）
- **P3** 统一"开关四点"生成器（一个声明表 → 自动出 `T_` 默认 / UI / 卸载）
- **P4** `[7]玩法` 拆独立块 + 统一 `Install/Uninstall` 接口
- **P5** 逐项清历史残留（每项先给证据、用户点头才动）

---

## 11.6.0 · 2026-09-24 —— V2 重置（规范 + 仓库 + 文件组织）

**不是功能更新，是一次「清场重开」**：按用户要求「全部重新写、不保留旧的东西、
避免以后的 AI 又去查以前的玩意」。

### 一、规范换成全新一套
- `AGENTS.md` **重写**：45,970 B → **16,461 B**。删掉全部版本沿革与过程稿，只留**当前应该怎样**：
  铁律 7 条 · **八层架构** · 开关接线「四点」 · 硬不变量（移动/过检测分离 · hook 卫生 · 界面即检测面 ·
  准星护栏 · 游戏事实 · 配色）· 发版流程 · 诚实边界 · 目录约定。
- `CLAUDE.md` / `.github/copilot-instructions.md` / `.cursor/rules/cheatmenu.mdc`
  ⇒ 改成**指向 `AGENTS.md` 的薄壳**（不再各自维护会发散的副本）。
- `README.md` 重写（用法 / 目录 / 规范指针 / 发版）。
- **源文件头**换成短版（29 行），开头即写明「完整规范 = AGENTS.md」。

### 二、仓库清掉全部旧产物（远端**删除 107 个文件**）
删除：38 份历史源码快照 · 20+ 篇逐版补强/候选/走查报告 · 原始抓包 txt ·
`文档存档/`（15 个旧 HTML 报告）· `ChronixHub-融合清单-v2.md` · `CHANGELOG.md.bak-*` ×2 ·
早期反复推送产生的重复事件库文档。
保留并整理：`事件库/游戏/` 6 篇**当前仍有用的游戏要点**（MM2 / DOORS / MachineParty /
对战服 FFA / KickALuckyBlock / 偷鱼），`事件库/README.md` 作索引；`CHANGELOG.md` 从本版开始记。

### 三、代码
- **本次不动功能逻辑**（只换文件头）。功能侧最近四版已修完：11.4.1「定在原地」·
  11.4.2 五处真 bug · 11.5.0 飞行/加速整块重写 + 防回退回归过检测 · 11.5.1 绝对速度滑块 + 自动兜底与移动自检。
- 静态清点（19,029 行）：**重复定义 0 · `T_` 死键 0 · `C_` 死键 0 · 断线开关 0** —— 接线层面是干净的。
- `luau-compile` **0 错误**。

### 四、下一步（等用户点头）
按 `AGENTS.md §2` 的八层把 19k 行**按层重排**：**P1「只搬不改」** —— 每层加边界锚点 + 生成模块索引，
并用「代码部分逐字符不变」自证。**逐层做、每层单独发版**，避免像 11.4.0 那样一次性大改出事故。
