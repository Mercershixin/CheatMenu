# 历史备份（旧版本带注释源码）

开发过程中留下的旧版本源码快照（`.bak-*` / `.keep-*`），从本地工作区搬进仓库，
目的是**源码历史不要只存在于一台机器上**（本机就发生过一次根目录文件意外消失）。

- 命名 `CheatMenu-<当时版本>.lua.bak-<那次改动名>`，后缀是那次改动的主题。
- ⚠ 它们是**历史文件，不是可运行的现行版本**；现行源码看上一级的 `CheatMenu-<版本>.lua`。
- ⚠ 未压缩的完整源码，比发行产物大（约 0.7–1.2 MB/个）。
